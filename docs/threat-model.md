# Threat Model

The canonical threat model lives in [`../SECURITY.md`](../SECURITY.md) §2.

This file exists as a placeholder so the path referenced in `CLAUDE.md` §
Repository Layout resolves. If a deeper analysis is needed for a specific
attack surface (a new adversary, a new dependency, a new data flow), extend
the table in `SECURITY.md` rather than duplicating it here.
