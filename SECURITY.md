# SECURITY.md — JHSC Worker Hub

Threat model, security controls, and incident response.

---

## 1. What We're Protecting

In order of sensitivity:

1. **Member identities and statements** — worker names, witness identities, statement bodies.
2. **Medical and accommodation records** — health info protected under PIPEDA and OHRC.
3. **Reprisal records** — narratives of supervisor conduct, dates, witnesses. Evidentiary value in OLRB.
4. **Recommendations and JHSC strategy** — drafts, adversarial analyses, predicted management responses.
5. **Evidence files** — photos, audio, documents.
6. **Practice journal entries** — rep-private strategic notes, separate key.
7. **Action item descriptions** — when they identify named workers, supervisors, or detailed workplace specifics, treated with same sensitivity as member statements.
8. **Excel import payloads** — imported workplace data is fully sensitive; treated identically to first-class records.
9. **Audit log** — proves chain of custody. Tamper would destroy evidentiary value of all records.
10. **Authentication credentials** — passkeys, password hashes, TOTP secrets, session tokens.

---

## 2. Threat Model

### Adversaries

| Adversary | Capability | Motivation |
|---|---|---|
| **Employer / management** | Access via employer infrastructure, social engineering, compelled disclosure | Discover worker-side strategy; identify members reporting hazards |
| **Hostile co-worker** | Physical access to a rep's phone/laptop, shoulder-surfing | Personal grievance, anti-union sentiment |
| **Opportunistic attacker** | Web attack surface — scanners, credential stuffing, exploits | Generic compromise |
| **Targeted attacker** | Sophisticated — phishing, supply chain, device theft | Anti-union motivation (rare but real) |
| **Compelled disclosure** | US court order to Fly or Neon under CLOUD Act; Canadian court order | Litigation discovery, criminal investigation |
| **Insider at provider** | Fly or Neon staff with administrative access | Negligence, curiosity, malice |
| **Compromised dependency** | npm package compromise, supply chain attack | Wide-scale exploitation |
| **Malicious Excel file** (NEW) | Crafted .xlsx/.xlsm with hostile content | Trigger parser bug, exfiltrate data, embed exploits |

### What's In Scope

- Web app at the production URL
- API at the production API URL
- Neon Postgres database
- Tigris file storage
- Fly Secrets (keys, credentials)
- Source code repository
- CI/CD pipeline
- Domain DNS
- **Excel import parser (SheetJS) and reconciliation engine**
- **Action item move workflow and audit chain anchors**

### What's Out of Scope (Documented Risk)

- Endpoint security on rep devices
- Social engineering of reps directly
- Coerced disclosure of a rep's auth credentials
- Physical theft of an unlocked device
- The integrity of the Excel files the rep chooses to import (if the rep imports a tampered file, the import will faithfully reflect what's in it; we cannot detect upstream tampering)

### 2.1 Auth-specific threats and mitigations (Milestone 1.2)

Concrete adversary actions against the authentication surface and how the 1.2 design (per ADR-0001) blocks or contains them.

| # | Threat | Adversary | Mitigation in 1.2 | Residual risk |
|---|---|---|---|---|
| T-A1 | Phishing the co-chair's password | Opportunistic / targeted | Passkey/WebAuthn primary path is phishing-resistant (origin-bound). Password fallback is gated by mandatory TOTP. | If the rep types both password and a fresh TOTP into a phishing site, attacker still needs to bypass the brute-force ladder on the real site. Acceptable; documented. |
| T-A2 | Credential stuffing on the password endpoint | Opportunistic | `login_attempts`-driven ladder: 5/15 min, 10/1 h, 20/manual. Constant-time response relative to credential verification to avoid an email-existence oracle. Rate limit 10 req/min/IP per `§3 Rate Limiting`. | Distributed attackers can hide under per-IP counters. Per-identifier counters catch this. |
| T-A3 | Session theft via XSS | Opportunistic / targeted | Tokens live in `__Host-*` HttpOnly Secure SameSite=Strict cookies. No `Authorization: Bearer` path. Strict CSP per `§3 Headers & Transport`. | If XSS lets the attacker make same-origin requests under the victim's session, the cookies will be sent — but only while the access JWT is valid (30 min). Step-up still required for sensitive actions. |
| T-A4 | Refresh-token replay after exfiltration | Targeted | Refresh tokens rotate on every use; reuse of a consumed refresh token is treated as compromise — the entire session row is killed and the user notified on next access. | Race window between use and rotation is sub-second. |
| T-A5 | JWT forgery | Targeted | EdDSA signing key in Fly Secrets only; `kid` header for rotation. Verifier only accepts known public keys. | Compromise of `AUTH_JWT_ED25519_PRIVATE_KEY_B64` is catastrophic — same blast radius as a DB-side session forge. Mitigated by Fly Secrets access controls. |
| T-A6 | WebAuthn challenge replay | Targeted | Challenges are single-use, 60-second TTL, stored server-side in `webauthn_challenges` and deleted on verify. UV is required on both registration and authentication. | None material. |
| T-A7 | Authenticator counter rollback | Targeted | `passkey_credentials.counter` is monotonic; any decrease vs. stored value fails authentication and flags the credential. | Authenticators that don't increment the counter (some platform authenticators) are accepted; documented. |
| T-A8 | TOTP brute force | Opportunistic / targeted | TOTP path inherits the `login_attempts` ladder. 30-second window with single-step skew tolerance only. | Negligible at ladder thresholds. |
| T-A9 | TOTP secret theft from DB | Insider / compromise | TOTP secrets encrypted at rest with the master key (via the 1.2 crypto stub; XChaCha20-Poly1305 in 1.3). Master key in Fly Secrets, never logged. | Master-key compromise compromises all stored TOTP secrets. Mitigated by Fly Secrets access controls + planned key rotation in 1.3. |
| T-A10 | Recovery-code theft | Insider / compromise | Codes stored as BLAKE2b hashes; only the user holds the plaintext (shown once at enrollment). | If the rep records codes in a compromised password manager, attacker can use them. Out of scope per §2 "Endpoint security." |
| T-A11 | First-run hijack | Opportunistic | `setup_state` singleton; the route returns 404 once `first_run_completed_at` is set. First-run does not require auth (you're bootstrapping) — but the route is on a fly-internal hostname during initial deploy if the operator follows the runbook. | If the production hostname is exposed before first-run completes, an attacker who reaches the URL first claims the co-chair account. **Runbook must require first-run before public DNS cutover.** |
| T-A12 | Step-up bypass | Targeted | `requireStepUp` middleware checks the access JWT's `step_up_until` claim. Claims are signed; the only way to forge one is T-A5. Step-up window default 5 min; export endpoints override to 60 s. | None at the auth layer. |
| T-A13 | Lockout used as a DoS against the rep | Targeted | Lockouts are scoped per-identifier *and* per-IP. A rep coming from a fresh IP can still authenticate even if their email is locked from another IP. **Manual-unlock (20-fail) tier is the residual DoS surface.** | The 20-fail tier requires a CLI ops action — documented in `docs/runbooks/auth.md` (follow-up). The rep can also authenticate via passkey path, which bypasses the password-side counters. |
| T-A14 | Audit gap during 1.2 → 1.3 window | Compelled disclosure / adversarial review | All auth events written to `auth_events` table in 1.2. 1.3's chained logger appends a backfill anchor whose payload is the SHA-256 of the canonical-JSON of those rows. Tamper of pre-chain rows is detected when the backfill anchor is re-verified. | Tamper *between* the event write and the 1.3 backfill anchor is undetectable. Window is the 1.2 → 1.3 calendar gap (≤ 2 weeks per ROADMAP). Accept and document. |
| T-A15 | Side-channel / timing oracle on email existence | Opportunistic | Password-path code does an Argon2id verify against a canary hash when the user doesn't exist, so latency does not distinguish "no such user" from "wrong password." Passkey path is discoverable-credential-first so no identifier is sent in plaintext at all. | None material. |
| T-A16 | Cross-origin/CSRF write via `__Host-*` cookies | Opportunistic | `SameSite=Strict` on all auth cookies. Mutating endpoints also require a custom header (`X-Requested-With: jhsc-web`) that simple CSRF forms cannot set. | None material. |

### Auth data flows (1.2)

```
[browser] --TLS--> [Hono /api/auth/*] --DB-->  users, sessions,
                                              passkey_credentials,
                                              password_credentials,
                                              totp_credentials,
                                              recovery_codes,
                                              login_attempts,
                                              auth_events,
                                              setup_state,
                                              webauthn_challenges
                                              (all in Neon ca-central-1)
                                              
                              \--Fly Secrets--> MASTER_KEY,
                                                AUTH_JWT_ED25519_PRIVATE_KEY_B64,
                                                AUTH_JWT_ED25519_PUBLIC_KEY_B64
```

No PI in transit beyond what the rep types (email at first-run, optional display name). No PI in JWT claims (only opaque `sub`, `sid`, `iat`, `exp`, `step_up_until`). No PI in `auth_events.metadata` (IP and UA only, plus typed event kinds).

### Trust boundaries (1.2)

- **Browser ↔ API:** untrusted on both sides of the wire; TLS 1.3 only; HSTS preload.
- **API ↔ Postgres:** API-side validates and authorizes every query; the DB role used by `apps/api` cannot read `auth_events` from a non-API session (enforced by row-level controls + a separate read-only role for the audit-verify script in 1.3).
- **API ↔ Fly Secrets:** read-only; the API never writes secrets. Secrets never appear in logs (the Pino redactor's allowlist already strips known secret keys; an additional regex catches `AUTH_JWT_*` and `MASTER_KEY`).

### 2.2 Audit-chain and crypto threats (Milestone 1.3)

Threats specific to the `packages/audit` tamper-evident chain and the `packages/crypto` wire format introduced in 1.3 (per ADR-0002).

| # | Threat | Adversary | Mitigation in 1.3 | Residual risk |
|---|---|---|---|---|
| T-AC1 | Tampered `audit_log` row body | Insider / compelled disclosure | `this_hash` covers every column in canonical-JSON order via SHA-256(`prev_hash || canonical_json(headers + payload)`). `scripts/audit-log-verify.ts` recomputes the full chain nightly via cron + on-demand via runbook §7. | Tamper is detected, not prevented; runbook §7 covers the response. |
| T-AC2 | Inserted "phantom" row breaks the chain | Insider / compelled disclosure | Every row's `prev_hash` is the previous row's `this_hash`. A row inserted out of band by SQL would have `prev_hash` matching its real predecessor and `this_hash` self-consistent for its own body, but the row AFTER it (next legit `append()`) would compute `prev_hash` from the phantom's `this_hash` and the chain integrity holds — except that `verify()` walks `idx` order and the phantom's `idx` must fit. A phantom with no later legit appends is detected by the next nightly verify when the table is empty downstream. | A clever insider could replace ALL downstream rows AND the phantom; mitigated only by off-host archival (1.3 §3a + 1.12 hardening). |
| T-AC3 | Gap (missing `idx`) | Insider / DB corruption | `idx` is `bigint primary key`, monotonic. `verify()` walks `idx ASC` and reports a gap as `firstDivergence`. Gaps are not crashes — operations continue but verify reports tamper, triggering runbook §7. | None at the application layer. |
| T-AC4 | Race between concurrent `append()` calls | n/a (operational) | `append()` runs `SELECT … ORDER BY idx DESC LIMIT 1 FOR UPDATE` inside the transaction, serializing appenders. Throughput is single-machine — fine for single-tenant. | Multi-rep concurrent writes (a future scope) would need an advisory lock or a single appender process. Documented in ADR-0002. |
| T-AC5 | Genesis-row replacement | Compromised migration | `idx=0` is inserted by the migration in an idempotent script that fails if `idx=0` already exists. `prev_hash` for genesis is `\x00 × 32`; `verify()` requires this at chain start. Replacement of genesis requires admin access AND would invalidate every subsequent `this_hash`. | None at the application layer. |
| T-AC6 | 1.2 → 1.3 backfill anchor tamper | Insider / compelled disclosure | Anchor row at `idx=1` carries `rows_sha256` over the canonical JSON of `auth_events` in `(ts, id)` order. A re-run of `verify()` recomputes that SHA-256 from the live `auth_events` and matches it. Tampering `auth_events` rows post-1.3 is detected at next verify. | Tamper between the 1.3 deploy and the first nightly verify is possible — narrow window, accepted. |
| T-AC7 | Crypto stub forward-read on a v=0x02 ciphertext | Misconfigured rollback | `open()` rejects unknown version bytes with `CryptoOpenError(unsupported_version)`. A rollback that re-runs 1.2 binaries against 1.3-written rows fails loud, not silent. | A rollback strategy must be paired with a re-encrypt-to-v0x01 dump — but rolling back from 1.3 to 1.2 is not supported anyway. |
| T-AC8 | KEK leak via subprocess argv | Operational | The new master-key rotation runbook (§3a) uses Fly Secrets only; the KEK never appears in argv to any rotation script. `packages/crypto` takes a `KeyProvider` interface so neither tests nor scripts need to env-read directly. | Operator error remains the residual; runbook calls it out. |
| T-AC9 | Payload PI leak | Implementer error | `packages/shared-types` exports per-`kind` discriminated unions for audit payloads. PI fields (email, displayName, plaintext body) are not declared on any union — the typechecker rejects them at every `append()` site. Runtime safety net: a JSON-schema reject layer (1.12 hardening) backs up the type-only check. | Without the runtime check (deferred to 1.12) a `kind` not yet typed could pass a PI string; type discipline catches the common case. |
| T-AC10 | Multi-kid JWT verifier rejects valid token at rotation | Operational | The kid registry accepts `legacy` for tokens without a kid suffix (1.2-compat). Rotation runbook §3 sequences `flyctl secrets set` for the new kid BEFORE flipping `AUTH_JWT_ACTIVE_KID`. | A misconfigured rotation that forgets the new public key still rejects in-flight tokens. Documented. |
| T-AC11 | `operator` field in `lockout.cleared` / `session.revoked` payloads leaks PI by linkage | Operational | The `operator` field carries `$(whoami)` from the admin CLI. In a single-tenant, single-co-chair deployment the OS user IS the co-chair, so the field is a self-identifier (not third-party PI). A future multi-rep scope (1.12+) revisits. Documented. | Privacy posture depends on the operator's account naming. Tracked. |
| T-AC12 | `audit_log` IP/UA retention exceeds PIPEDA P5 floor | Operational | Documented in `.context/decisions.md` 2026-05-29 entry: indefinite chain retention with annual IP/UA redaction sweep + `audit.ip_redacted` chain anchor. Sweep script is a 1.12 hardening line item. Until it lands, IP/UA persist for the full chain age. | Documented residual; 1.12. |

### 2.3 Auth + crypto-chain integration threats

| # | Threat | Mitigation | Residual |
|---|---|---|---|
| T-AI1 | TOTP reset endpoint abused to reset a victim's TOTP | TOTP reset is step-up-gated. Step-up requires either passkey or current TOTP. The attacker would need to already control one of these factors. | None material. |
| T-AI2 | Step-up modal bypass | Modal opens on a 401-StepUp from `stepUpEmitter`. Server is the source of truth; modal cannot self-claim grant. Server re-issues the access JWT with `step_up_until` claim only after a verified factor. | None material. |
| T-AI3 | KEK rotation while sessions are live | Session refresh re-derives email lookup hashes from the NEW KEK. Tokens issued before rotation continue to validate (access JWT signing keys are independent); first refresh after rotation pins the new KEK. Runbook §3a sequences rotation during a low-traffic window. | Sessions issued just before rotation may see one transient lookup failure during the rotation window. Acceptable; documented. |

### 2.4 Corpus integrity threats (Milestone 1.4)

The legal corpus (OHSA, O. Reg. 851, CLC Part II, COHSR) underlies citation rendering, recommendation linkage, and the `/legal` search surface. Wrong text shown to a JHSC member can produce non-compliant recommendations; stale text shown against an old `version_date` can mis-anchor a historical recommendation. The clauses table is the trust anchor — both for what readers see today and for what future readers see when chasing a past audit citation.

| # | Threat | Mitigation | Residual |
|---|---|---|---|
| T-LC1 | Wrong clause served — e.g. `OHSA s.25(2)(h)` returns `s.25(2)(c)` due to a seeder ordering bug or a hand-edit | Seeder is the **only** writer (no admin write endpoint). Every clause is keyed on `(statute_id, citation, version_date)` UNIQUE. `<CitationRef />` resolves via `(statute, citation)` against the **active** corpus_version; recommendation reads pin `(citation, version_date)` so historical recs cannot be re-anchored by a re-seed. Seeder runs a post-load sweep that asserts `(statute_id, citation, version_date)` count matches the fixture row count. | A fixture authoring error (wrong body for the right citation) is not caught by structural checks; mitigated by 2-person fixture review documented in `docs/runbooks/legal-corpus.md`. Acceptable. |
| T-LC2 | FTS index poison — `tsvector` index returns a clause that does not contain the search terms, or hides one that does | `search_tsv` is a `GENERATED ALWAYS AS (...) STORED` column derived from `(heading, body, citation)`; it cannot be set out-of-band. The expression is fixed in migration 0002 and any change is a new migration reviewed under the SQL-migration policy. Search route returns `(clause_id, citation)` only; the client re-fetches the body through the canonical `GET /api/legal/clauses/:id` so a poisoned ranking cannot ship the wrong body. | A skewed ranking can still bury the right answer; UI surfaces full `citation` and `version_date` so the reader can disambiguate. Acceptable. |
| T-LC3 | Hash-anchor mismatch on recommendation read — recommendation references a clause body that has been superseded by a re-seed | `body_hash = SHA-256(body \|\| version_date.toISOString())` is stored on every clause row. Recommendation rows persist `(clause_id, body_hash)` at write time. Recommendation read API joins on `body_hash` and returns a `clause_superseded` flag when the live row's hash differs, with a `superseded_by` pointer so the reader can see both the original and the current text. Re-seed updates never UPDATE an existing row; they INSERT a new `(statute_id, citation, version_date)` and set the prior row's `superseded_by`. | Reader is shown both versions; mitigation is detection, not silent rewrite. Acceptable. |
| T-LC4 | Copyright violation via summary→full_text drift — a `third_party_restricted` statute (CSA, ISO, ACGIH) lands in the DB with `body_kind='full_text'` | Structural copyright guard in the seed loader: any fixture with `statutes.licence='third_party_restricted'` and `clauses.body_kind='full_text'` fails the seeder with exit 1 before any row is written. Reinforced by a DB CHECK constraint on the clauses table: `body_kind = 'full_text' IMPLIES (SELECT licence FROM statutes WHERE id = statute_id) = 'crown_copyright_open'`. Implemented as a trigger because Postgres CHECK can't subquery. | A fixture author who alters the statute's licence row in the same commit can defeat the trigger; mitigated by the 2-person fixture review and a CI grep for `licence = 'third_party_restricted'` diffs on PRs that also touch clauses fixtures. Acceptable. |
| T-LC5 | Re-seed accidentally overwrites historical text — operator runs the seeder against a checked-out older fixture set and the live `version_date` rows get rewritten | Seeder never issues UPDATE on clauses; it is INSERT-only. A re-seed with the same `(statute_id, citation, version_date)` triggers the UNIQUE constraint and aborts the transaction. To publish a correction at the same `version_date`, the operator must INSERT a new `version_date` (one day later, with a `correction_of` pointer) — there is no in-place edit path. | Seeder failure surfaces as an exit code and a `audit.corpus.seeded` chain event is NOT emitted for the failed run; runbook §4 covers the recovery procedure. Acceptable. |
| T-LC6 | `corpus_versions` ledger tamper — attacker rewrites the ledger so the active version points at stale text | `corpus_versions` rows are referenced by `clauses.corpus_version` via FK with `ON DELETE RESTRICT`. The active version is selected by `MAX(activated_at)` over rows where `retired_at IS NULL`. Each `corpus_versions` insert emits an `audit.corpus.seeded` chain event with `{version, statutes[], clause_count, fixture_sha256}`; tampering with the ledger leaves the chain event in place, and `audit-log-verify` flags any version row whose `fixture_sha256` does not match the chain anchor. | Detection-only; an attacker with DB-write can both rewrite the ledger AND prepend a fake chain event, but the chain's `prev_hash` linkage means re-anchoring requires rewriting every subsequent event (same property the audit-chain already relies on). Acceptable. |
| T-LC7 | `<CitationRef />` renders a citation that no longer exists in the active corpus | Component server query is `SELECT id, body, body_kind, body_summary, version_date FROM clauses WHERE statute_id = $1 AND citation = $2 AND corpus_version = $active_version LIMIT 1`. On zero rows, renders a `<MissingCitation citation={...} />` placeholder with a link to the `/legal` search surface; does NOT silently fall back to a different `version_date`. Recommendation editor's citation picker validates against the active corpus before save, so authoring a missing citation requires going around the UI. | Hand-authored content (markdown body) can reference a removed citation; renderer shows the placeholder. Acceptable. |
| T-LC8 | Search endpoint exfiltrates restricted body text — `third_party_restricted` summary rows leak full text via aggregation | Search route projects `(clause_id, citation, version_date, heading, body_kind, ts_rank)` plus a `snippet` derived ONLY from `(heading, body_summary)` for restricted rows and from `(heading, body)` for crown-open rows. The `body` field is never returned by the search route. `GET /api/legal/clauses/:id` returns `body` only when `body_kind='full_text'`; for `body_kind='summary'` it returns `body_summary` and a `source_url`. | A reader can reconstruct partial text via many narrow searches against a restricted summary; surface area is bounded to the summary text only, which is the JHSC's own paraphrase. Acceptable. |

**Audit hooks for corpus changes.** The audit-chain emits two corpus event kinds (`audit.corpus.seeded`, `audit.corpus.amended`) with no payload fields containing PI. Payload schema is fixed in `packages/shared-types` and enforced at the typed-emit boundary (priv-F2 design from Milestone 1.3 extends to corpus events). Verification: `audit-log-verify --check-corpus` cross-checks every `corpus.seeded` event's `fixture_sha256` against the live `corpus_versions` row.

**Close-out review findings (Milestone 1.4 slice 5).**

| Finding | Where landed |
|---|---|
| sec-F1 — XSS via fixture body → `ts_headline` snippet | Web renderer switched off `dangerouslySetInnerHTML` to a strict `<mark>`-split renderer (`apps/web/src/views/legal-view.tsx` `SnippetRenderer`); Zod fixture schema rejects `<` `>` in `body` / `heading` / `body_summary` (`packages/legal-corpus/src/fixtures.ts`). |
| sec-F2 — statute UPDATE flipping licence orphans full_text clauses | `statutes_copyright_guard_trigger` added in migration 0003 — refuses the UPDATE when matching clauses exist. |
| sec-F3 — `sql.raw` for `hierarchy_path` | Seeder switched to `sql.join` over parameter-bound elements (`apps/api/scripts/seed-legal-corpus.ts`). |
| sec-F4 — no rate limit on `/api/legal/search` | Token-bucket middleware (`apps/api/src/middleware/rate-limit.ts`): `/search` 20 burst / 5 rps, `/clauses` + `/statutes` 60 burst / 20 rps, keyed by `Fly-Client-IP`. |
| sec-F5 — partial re-seed orphans existing statutes | Read routes now filter `superseded_by IS NULL` instead of `corpus_version = active`; seeder refuses to drop a previously-loaded statute without `--allow-statute-removal`. |
| sec-F6 — restricted body text reachable via FTS oracle | `search_tsv` rebuilt in migration 0003 as licence-aware (`body_summary` for `summary` rows; `body` for `full_text`); DB CHECK `body_kind='full_text' OR body_summary IS NOT NULL`. |
| priv-F1 — referrer policy on web origin | `<meta name="referrer" content="no-referrer">` added to `apps/web/index.html` as defence-in-depth for the existing `rel="noreferrer"` anchors. |
| priv-F2 — `verified_by="kdm"` is operator initials | All four seed fixtures + Zod test fixture changed to `verified_by="corpus-operator"`; out-of-band operator log in `docs/runbooks/legal-corpus.md` §3. |
| priv-F3 — OHSA s.9(20) text may be mislabelled | Privacy reviewer flagged a possible swap between s.9(18) and s.9(20); reviewer could not independently fetch e-Laws (network policy blocks it). Mandatory 2-person verification gate now blocks Milestone 1.9 ship — `docs/runbooks/legal-corpus.md` §2. |
| priv-F5 — `corpus_versions.operator` column dropped from ADR | Implementation divergence documented in ADR-0003 §"Implementation divergences"; runbook §3 records operator identity out-of-band. |
| priv-F6 — `/api/legal/*` rode auth cookies | Web client `apps/web/src/legal/api.ts` switched to `credentials: 'omit'`. |

### 2.5 Hazards threats (Milestone 1.5)

Hazards are the first DB surface that carries worker-personal content: rep-authored descriptions of conditions, reporter identity when not anonymous, and (optionally) location detail naming a specific worker's station. Encryption applies at the column level via the `@jhsc/crypto` envelope (1.3). The hazard lifecycle is chain-anchored — every status transition emits `hazard.status_changed`, every create emits `hazard.created` — so the JHSC-discipline question "who closed H-47 and why" has a tamper-evident answer in `audit_log`.

| # | Threat | Mitigation | Residual |
|---|---|---|---|
| T-H1 | Description ciphertext exfil via DB compromise reveals worker concerns | All four sensitive columns (`description`, `reporter_identity`, `location_detail`, `status_history.reason`) are sealed with per-row DEKs under the workplace KEK. Postgres sees ciphertext only; KEK lives in Fly Secrets. CLAUDE.md §"Encryption Rules" honored. | DB-level attacker with `pg_dump` access still gets ciphertext; KEK is the bound. Acceptable; same as 1.3. |
| T-H2 | Status-workflow skew lets a hazard skip the JHSC's accountability path (e.g. `open → archived` direct) | The allowed-transitions graph is a pure-function helper (`apps/api/src/hazards/transitions.ts`). The PATCH route validates the requested transition against the graph before writing; an invalid transition returns 422 with the rejected pair. Tests assert every legal transition AND that a representative sample of illegal transitions (`open → resolved`, `open → archived`, `resolved → open`) is rejected. | A handler bug could still drift the graph — covered by the unit tests on the pure helper. Acceptable. |
| T-H3 | Withdrawn used as a delete escape valve to erase an inconvenient hazard | `withdrawn` is the only "cancel" path; it is fully audited (chain event + encrypted reason) and the row stays in the DB. Step-up required for the `→ withdrawn` transition. Restore-from-withdrawn is not supported in 1.5; an operator who wants to revive a hazard re-creates it with a `linked_to_withdrawn` pointer (1.6). | A rep with their own step-up credential can still withdraw a hazard, but the chain row preserves who/when/why. Acceptable. |
| T-H4 | Reporter identity over-disclosure on `GET /api/hazards/:id` | The route returns `reporter_identity` plaintext only after a step-up check (same modal pattern as 1.2/1.3). List route never includes reporter identity in any form. The intake form's "report anonymously" checkbox sets `reporter_identity_ct = NULL`. | Step-up bound; an authenticated rep with their own factors can decrypt. Acceptable in the single-co-chair scope (CLAUDE.md). |
| T-H5 | Title field PI leakage — rep types a real name into the non-encrypted title | Title is bounded to 120 chars and the intake form's placeholder/help text directs to non-PI summaries (e.g. "Slip hazard — cooler floor", not "John's complaint about cooler"). Zod schema rejects titles longer than 120; no server-side PI scrubber. | A rep can still type a name; the intake-form copy is the bound. Documented; depends on rep discipline. |
| T-H6 | Search query log leaks intended content (`?q=Jane reported assault`) | The list route runs FTS over title only. Title is bounded as above. The search query is not persisted; access logs are policy-scoped per `docs/runbooks/auth.md` retention rule. | Access-log retention is the bound. Acceptable. |
| T-H7 | Audit chain emits PI via the hazard payload | `hazard.created` payload carries `{hazardId, hazardCode, severity, jurisdiction}` only. `hazard.status_changed` carries `{hazardId, hazardCode, fromStatus, toStatus}` only. No title, no description, no reporter. Reasons live in the encrypted `hazard_status_history.reason_ct` column, not the chain row. Same priv-F2 invariant as 1.3: typed payloads enforced at the `append()` boundary. | None material — the payload shape is fixed by the union type in `packages/shared-types`. |
| T-H8 | Status transition race — two reps PATCH the same hazard simultaneously | The PATCH route opens a Drizzle transaction, SELECTs the hazard's current status with `FOR UPDATE`, checks the transition, writes the new status + history row + audit append. The pg_advisory_xact_lock in `@jhsc/audit append()` serializes the chain side. The hazards-row lock serializes the row side. | Two PATCHes for different status moves still appear ordered; the second sees the first's status as the "from" and is rejected if illegal. Acceptable. |
| T-H9 | Hazard creation succeeds but chain emit fails — orphaned chain row OR orphaned hazard | Hazard insert + chain emit run inside one `db.transaction`. A failure on either rolls back both. The audit chain's PK-collision design (1.3 sec-F1) cannot leave a half-row. | Verified by integration test (S2). Acceptable. |
| T-H10 | Severity is selectable by the reporter, allowing under-rating to suppress urgency | Severity is the reporter's call by design — the rep is the SME and the JHSC reviews on assess. Status `assessing` is the explicit "JHSC reviews and may change severity" stage. Severity change inside `assessing` is a normal write; severity change after `assigned` requires the rep to transition back to `assessing` first. | Acceptable — matches OHSA s.9(20) workflow; the JHSC owns the severity call. |
| T-H11 | Hazard code (`H-NNN`) leaks workplace size via monotonic count | `H-NNN` is monotonic within a workplace. A list of hazard codes reveals approximate total count of reports filed. The codes are visible to any authenticated rep — same scope as the rest of the data. | Single-tenant deployment; the audience for hazard codes IS the JHSC. Acceptable. |
| T-H12 | KEK rotation while a status transition is in flight | Status transitions don't touch `reason_ct` of prior rows — they INSERT a new history row. The crypto-rewrap path (1.3) is the bound; a rotation runs the standard KEK rotation procedure (`docs/runbooks/auth.md` §3a), which already covers in-flight encrypted writes. | Same bound as 1.3 KEK rotation; documented. |

**Audit hooks for hazard changes.** Two new event kinds (`hazard.created`, `hazard.status_changed`) added to `packages/shared-types` `AuditEventKind`. Both payloads pass through the same typed-emit boundary as 1.3 — `emitAuthEvent`'s 1.3 invariant generalizes to all writers (priv-F2 from 1.3 carries forward).

**Close-out review findings (Milestone 1.5 slice 5).**

| Finding | Where landed |
|---|---|
| sec-F1 — no body-size limit or rate cap on `/api/hazards` | `bodyLimit({maxSize: 64 * 1024})` mounted on `hazardsRoute.use('*')` (returns 413 over the cap); existing token-bucket `rateLimit` middleware applied at `60 burst / 10 rps` per-IP. |
| sec-F2 — illegal_transition / step-up_required returned inside the FOR UPDATE transaction (held the row lock for response serialization) | Body parse + initial transition graph check + step-up freshness check moved OUTSIDE the transaction. Race-conditioned writes inside the lock throw `HazardWriteAborted` to roll back cleanly; the outer handler maps to the right JSON response. |
| sec-F3 — PATCH inline step-up bypassed the freshness floor + omitted `max_age` | New exported `checkStepUpFreshness` in `apps/api/src/auth/step-up.ts` is the single source of truth for the freshness check. The PATCH handler calls it with `maxAgeSeconds: 60` (destructive transition) — a re-step-up older than one minute is rejected and the WWW-Authenticate header carries `max_age="60"` so the web modal can warn correctly. |
| sec-F5 — list path crashed on a single corrupted ciphertext | Per-row `try/catch` around `openField`; bad rows surface as `summary: '[unreadable — open the detail view for diagnostics]'` so the rest of the list stays usable across KEK rotation edge cases (T-H12 in-flight). |
| sec-F6 — ILIKE wildcard injection (no security severity, but breaks the search contract) | `q` is escaped against `\`, `%`, `_` before pattern construction; query carries `ESCAPE '\'`. |
| sec-F7 — H-NNN sequence is non-contiguous (failed transactions leave gaps) | Documented in `docs/runbooks/hazards.md` §2 ("Hazard codes — non-contiguous by design"). Treated as an acceptable mitigation of T-H11. |
| priv-F1 — intake form copy misled about the encryption boundary | Caption replaced with "travel to the server over HTTPS, and are encrypted at rest with a key held by the workplace before they are written to the database." Empty-state copy similarly clarified. |
| priv-F2 — no runbook for hazards / PIPEDA right-to-erasure procedure | `docs/runbooks/hazards.md` lands covering schema overview, H-NNN non-contiguity, withdrawn lifecycle, PIPEDA Principle 9 response procedure (refuse-or-redact based on activity + retention + named-individual paths), KEK rotation impact, and tamper response. |
| priv-F4 — `safeSummary` returned 80 chars verbatim when no word boundary exists | Falls back to `(cap - 10)` when no usable space is in the prefix, shedding the trailing partial token. Seven new unit tests cover the boundary cases (`apps/api/src/hazards/crypto.test.ts`). |

### 2.6 Action items threats (Milestone 1.6)

Action items are the operational primitive of the Minutes module (CLAUDE.md non-negotiable #12) and the convergence point for hazards (1.5), recommendations (1.9), inspections (1.8), incidents (later), and Excel-imported historical items (1.11). The `_MoveHistory` sheet from the legacy spreadsheet workflow is replaced by `action_item_moves` — every section change is chain-anchored. Four envelope-encrypted fields carry the PI-bearing surfaces (description, recommended_action, raised_by external, follow_up_owner external). The Action Flag pure function (ARCHITECTURE.md §5) computes the 21-day s.9(21) indicator from canonical date columns at read time — no stored state to drift.

| # | Threat | Mitigation | Residual |
|---|---|---|---|
| T-AI1 | Description / recommended_action ciphertext exfil via DB compromise reveals worker concerns and management strategy | All four sensitive columns (`description`, `recommended_action`, `raised_by`, `follow_up_owner`) and `action_item_moves.reason` are sealed with per-row DEKs under the workplace KEK. Postgres sees ciphertext only. CLAUDE.md §"Encryption Rules" honored. | DB-level attacker still gets ciphertext; KEK is the bound. Same as 1.3/1.5. |
| T-AI2 | Section move bypasses the audit chain — a manual SQL UPDATE on `action_items.section` would skip the move row + chain anchor and break the "every move is logged" CLAUDE.md guarantee | The API has NO path that writes `action_items.section` outside the `POST /api/action-items/:id/moves` handler, which always emits the move row + chain anchor in one transaction. A manual SQL UPDATE is not blocked by Postgres CHECK (can't reference another table) but the audit-chain verify pass catches the divergence at read time. | Detection-only at the DB level. Documented in the runbook tamper procedure. |
| T-AI3 | Move-graph skew lets an item skip the workflow (e.g. `new_business → archived` direct) | The section transition graph is a pure function (`packages/shared-types/src/action-item-transitions.ts`); the route validates against it before writing. Tests assert legal/illegal pairs exhaustively. | A handler bug could drift the graph — covered by unit tests on the pure helper. Acceptable. |
| T-AI4 | Undo loop / chain-flood — a compromised credential spams moves + undoes to grow the chain or destabilize the move history | Rate-limit applies (`hazards` shape: 60 burst / 10 rps). Undo is step-up gated (`maxAgeSeconds=60`). The chain rows carry no PI, so growth is cheap in size; the operational concern is move-history readability, which the UI bounds via a "show undone" toggle. | Operational; rate + step-up are the bounds. |
| T-AI5 | Sequence-number collision under concurrent inserts | Per-section sequence is computed as `MAX(sequence_number) + 1 WHERE section = $1` inside the create transaction with a section-scoped row lock (advisory or SELECT MAX … FOR UPDATE). Two concurrent inserts serialize; rollback does not advance (unlike a Postgres sequence) so re-tries get the same next number. | If MAX-locking turns out to be a hotspot at scale (>10 inserts/sec into one section), revisit with a per-section sequence + a uniqueness CHECK. Single-co-chair scope: never. |
| T-AI6 | Audit chain emits PI via the action_item payload | Payload union in `packages/shared-types` restricts `action_item.created` to `{itemId, type, section, risk}`, `action_item.updated` to `{itemId, changedFields[]}` (a fixed allow-list of field names, never values), `action_item.moved` to `{itemId, fromSection, toSection, undone?}`, `action_item.move_undone` to `{itemId, movedItemId, revertedFromSection, revertedToSection}`. No descriptions, no names, no department text. Reasons live in encrypted `reason_ct`. Same priv-F2 invariant as 1.3 carries forward. | None material. |
| T-AI7 | Action Flag drift — a UI that computes the flag client-side disagrees with the server's canonical clock | The pure-function flag is computed server-side and returned in every list/detail projection. The web client renders the returned label/severity verbatim; it doesn't re-compute. Tests assert the flag matches the ARCHITECTURE.md §5 truth table for boundary inputs. | Client cache staleness across day boundaries — a list cached at 23:59 reads as < 21 days but the next morning is > 21. Acceptable; the rep refreshes. |
| T-AI8 | Polymorphic source FK (`source_type` + `source_id`) cannot be FK-enforced — an action item could link to a hazard id that doesn't exist | `'hazard'` source is enforced by the `action_items_source_fk_guard` trigger in migration 0005 (verifies the hazard row exists at INSERT/UPDATE). `'recommendation'` / `'inspection'` / `'incident'` are REJECTED at the route layer until their owning migration lands the equivalent trigger (sec-review F7 + priv-AI-F3 1.6 — fail-closed forward seam). `'manual'` and `'excel_import'` carry no referential integrity by design. | Race: hazard deleted between create-check and insert — bounded by `hazards.ON DELETE RESTRICT`. Acceptable. |
| T-AI9 | Closed action items leak management strategy when read across reps | Same scope as the rest of the encrypted surface — every authenticated rep sees the same data. Single-co-chair deployment makes this acceptable. The recommendation 1.9 flow will introduce a "draft" visibility tier; action items inherit the multi-rep forward seam from hazards T-H10. | Documented forward seam. |
| T-AI10 | Move-undo path used to whitewash an inconvenient move (mark it `undone`, hide from history view) | The undone flag does NOT delete or modify the original move row; it sets `undone=true` and writes a fresh REVERTING move row, both chain-anchored. The UI's default move history shows ALL rows including undone; the "hide undone" toggle is an explicit user choice. | UI policy; the chain holds the truth. Acceptable. |
| T-AI11 | KEK rotation while a move is in flight | Move writes never touch ciphertext of prior columns — they INSERT a new move row and (optionally) update `action_items.section` + `updated_at`. The 1.3 KEK rotation runbook (§3a) bounds in-flight encrypted writes. | Same as 1.3/1.5 KEK rotation; documented. |
| T-AI12 | Pre-1.10 `meeting_id` is nullable text with no FK — an item could pin a meeting id that never exists | Pre-1.10 the column accepts NULL or any uuid; route doesn't validate beyond format. The 1.10 meetings migration adds the FK as `ON UPDATE RESTRICT ON DELETE SET NULL` and includes a backfill check that flags orphans. | Documented integrity bound; the 1.10 migration is the close-out. |
| T-AI13 | `raised_by` / `follow_up_owner` external names are returned on `GET /:id` without a step-up gate (deliberate departure from 1.5 T-H4 reporter-identity pattern) | The hazards `reporter_identity` (1.5 T-H4) is the identity of a *worker who reported* a safety concern — high reprisal risk, low operational need to render on every detail view, so it sits behind a separate step-up route. Action items' `raised_by` / `follow_up_owner` are the minutes' **"Raised by"** and **"Follow-up owner"** columns — operational metadata that the JHSC team reads in every meeting agenda and exported PDF. Gating these behind step-up on every detail view would break the core minutes workflow. The fields stay envelope-encrypted at rest (T-AI1), are returned only to authenticated reps over HTTPS with `same-origin` credentials, and rendered in the web view with an "encrypted at rest" affordance (`apps/web/src/views/action-item-detail-view.tsx`). | Same scope as T-AI9 — multi-rep teams revisit when role separation lands. Acceptable. |

**Audit hooks for action-item changes.** Four event kinds — `action_item.created`, `action_item.updated`, `action_item.moved`, `action_item.move_undone` — added to `packages/shared-types` `AuditEventKind`. Move events are the operational heartbeat: under heavy traffic this is the largest single contributor to `audit_log` size. Payloads are tiny (≤4 fields each, no PI), so storage cost stays bounded.

**Close-out review findings (Milestone 1.6 slice 5).**

| Finding | Where landed |
|---|---|
| sec-F1 / priv-AI-F2 — `closedDate` PATCH writes bypassed the audit chain (HIGH) | `closed_date` added to `actionItemUpdateField` allow-list in `packages/shared-types/src/index.ts`. PATCH handler refactored so the SET-parts and the `changedFields` audit payload are built from the SAME table (`PATCH_TABLE` in `routes/action-items/index.ts`), preventing a future contributor from adding a column that bypasses the chain again. |
| sec-F2 — section move violated `(section, sequence_number)` UNIQUE index (HIGH, merge-blocker) | New `allocateSequenceNumber(tx, section)` helper used on every path that places an item into a section: create, move, and undo. Re-allocates the per-section "#" on move so an inbound item never collides with an existing item in the destination section. Matches the Excel workflow (a row moved between sheets gets that sheet's next row number). |
| sec-F3 — schema/audit drift seam (MED) | Closed by the `PATCH_TABLE` shape from sec-F1; new columns now require an entry in the single table or the typechecker fires. |
| sec-F4 — bodyLimit ordering let oversize POSTs bypass the rate-limit bucket (LOW) | Middleware re-ordered so `rateLimit` runs BEFORE `bodyLimit`. A spammed >64KB POST still drains the token bucket; the 413 follows. |
| sec-F5 — dead `escapedQ` (LOW) | Removed; the post-decrypt JS `.includes()` filter is escape-immune and the vestigial code is gone. |
| sec-F6 — `q` filter only matches the 80-char `safeSummary` (LOW, UX/spec) | Documented in the runbook as a known limitation; the API behavior matches ADR-0005's "ILIKE over a decrypted-then-trimmed description preview." No code change. |
| sec-F7 / priv-AI-F3 — T-AI8 overstated FK-validation for non-hazard sources (MED) | Route Zod schema now REJECTS `sourceType IN ('recommendation', 'inspection', 'incident')` until their owning migration lands the equivalent trigger. SECURITY.md T-AI8 row tightened to match the actual implementation (fail-closed). |
| priv-AI-F1 — `raised_by` / `follow_up_owner` return without step-up gate (drift from T-H4) | Deliberate departure documented as T-AI13: action items are minutes-sheet metadata, not reprisal-risk reporter identity. Rendered with "encrypted at rest" affordance. |
| priv-AI-F4 — `movedByUserId` UUID + sequence count oracle + dates-as-PI (info) | Documented as T-AI13 / T-AI9 residuals; forward-seam for multi-rep teams. |
| priv-AI-F5 — `docs/runbooks/action-items.md` missing (PIPEDA P9 procedure) | Runbook lands covering schema overview, sequence-number per-section semantics, withdraw / cancellation lifecycle, PIPEDA Principle 9 response procedure (refuse-or-redact based on activity + retention + named-individual paths), KEK rotation impact, and tamper response. |
| priv-AI-F6 — detail-view encryption affordance didn't cover `raised_by` (LOW) | Lock icon + "encrypted at rest" tag added next to the rendered Raised-by line in `apps/web/src/views/action-item-detail-view.tsx`. |

### 2.7 Evidence threats (Milestone 1.7)

Evidence files are the first surface that ships **client-side encryption** in production, and the first that uses object storage (Tigris). The browser holds the workplace **public** key (sealed-box recipient); the workplace **private** key stays sealed under the KEK in Fly Secrets, opened only when the server needs to decrypt for a authorized read. Every upload + read is chain-anchored.

| # | Threat | Mitigation | Residual |
|---|---|---|---|
| T-E1 | Tigris bucket compromise reveals ciphertext blobs and metadata | Files are XChaCha20-Poly1305-encrypted in the browser before upload. Per-file DEK is sealed for the workplace public key via `crypto_box_seal`; the server holds only the sealed DEK. Bucket holds opaque ciphertext + the 5-min presigned-URL ACL. | Even with bucket access, attacker needs (a) workplace private key (sealed under KEK in Fly Secrets) AND (b) chain-row metadata (`evidence_files.sealed_dek`). KEK rotation procedure documented. |
| T-E2 | Plaintext photo on the server during upload | Two-step upload: browser PUTs ciphertext directly to Tigris via presigned URL; the API never sees plaintext during upload. Server only receives metadata + sealed DEK in the `POST /api/evidence` finalize call. | None at upload time. **Decrypt path (T-E3)** is the documented exception. |
| T-E3 | Plaintext photo on the server during decrypt-and-read (`GET /api/evidence/:id/decrypt-url`) | Path A from ADR-0006: API opens sealed DEK with the workplace private key, decrypts the ciphertext, streams plaintext back. Plaintext is bounded to a single response lifetime; no caching, no logging. Step-up auth gates the call for evidence linked to worker-identity-bearing entities. | Acceptable for single-tenant scope; Path B (Cloudflare-Worker filter) deferred. Documented. |
| T-E4 | EXIF / camera-roll PI leaks via uploaded photos | Browser captures via `getUserMedia` → `<canvas>` re-encode → blob; the canvas re-encode path drops EXIF for fresh JPEG/PNG outputs. Camera roll is never read (capture happens in-page, not via `<input type="file" accept="image/*" capture>` which would route through OS picker). | iOS Safari HEIC re-encode has a known quirk where some EXIF survives the canvas round-trip; runbook flags this. A 1.12 hardening sweep verifies fresh-capture outputs are clean. |
| T-E5 | GPS precision becomes a worker-tracking surface | `gps_latitude` / `gps_longitude` capped at NUMERIC(8,4) — ~11m resolution. Sub-station precision is intentionally lost. | A determined rep can still cross-reference 11m precision against a known facility layout. Same scope as the rest of the data; single-co-chair tenant is the audience. |
| T-E6 | Polymorphic `(linked_type, linked_id)` accepts FKs that don't exist | Same fail-closed shape as 1.6 priv-AI-F3: route layer rejects `linked_type IN ('recommendation', 'inspection_finding', 'incident')` until their owning migrations ship. Only `'hazard'` and `'action_item'` are accepted in 1.7. Both have route-level existence checks. | Same forward seam as action items. Documented. |
| T-E7 | Audit chain emits PI via evidence payload | `evidence.uploaded` payload carries `{evidenceId, linkedType, linkedId, mimeType, byteSize, plaintextSha256}`. `plaintextSha256` is a one-way hash, not PI. No filename, no GPS, no caption. `evidence.read` is structurally identical without the hash. Same typed-payload invariant as 1.3 priv-F2. | None material. |
| T-E8 | Workplace key pair rotation invalidates all prior sealed DEKs | Pre-rotation procedure: run a one-shot script that opens each `evidence_files.sealed_dek` with the OLD workplace private key, re-seals under the NEW workplace public key, writes back. Script lands in 1.12 hardening; auth runbook §3a documents the prereq. **Until then, the workplace key pair is treated as a permanent identity.** | Operational; documented. |
| T-E9 | Presigned URL leak via referrer or browser history | Presigned PUT URLs carry a 5-min expiry. The browser issues the PUT via `fetch` — no referer header propagation, URL not added to history. If a URL is intercepted in-window the attacker has 5 min to PUT arbitrary content under the prearranged storage key; the finalize step will reject because `ciphertext_sha256` won't match what the legitimate browser computed. | 5-min window + SHA-256 verification at finalize. Acceptable. |
| T-E10 | Voice-to-text routes audio to a third-party service | App calls only the browser-native `SpeechRecognition` / `webkitSpeechRecognition` API (CLAUDE.md non-negotiable #3 — no third-party SDK or remote ASR endpoint reached by our code). **However**: Chrome / Edge implement `webkitSpeechRecognition` by forwarding audio to Google's cloud STT — that's the browser's behavior, not ours. The `<VoiceToText />` UI surfaces this disclosure unconditionally when the mic is available so the rep can choose to hand-type sensitive narratives instead. | Browser-vendor STT routing where the platform implements the API that way. Rep has explicit notice + textarea-only fallback for sensitive content. |
| T-E11 | Evidence file is misfiled (linked to the wrong hazard/action item) | No DELETE endpoint. Misfilings are corrected via a future `evidence_redactions` table (1.12 hardening) that records the redaction + emits a chain anchor. Until then, the operator's option is to leave the misfiled evidence in place and add a note on the new linked entity. The chain row preserves the original linkage. | Documented operational limitation. The runbook covers the manual-note workaround. |
| T-E12 | Large file DoS — 50 MB cap is the only bound | Per-file `byte_size <= 50 * 1024 * 1024` at schema + bucket policy + Zod refinement. Per-IP rate-limit applies on `/api/evidence/upload-url` (same 60 burst / 10 rps shape as 1.5/1.6) so a malicious rep can't request a thousand presigns in a second. Tigris bucket has its own per-key quotas. | Single rep is the failure mode; rate limit is the bound. Acceptable. |
| T-E13 | List endpoint metadata read (GPS + timestamps + uploader id) bypasses the step-up anchor that decrypts use | Per priv-F5 close-out: `GET /api/evidence?linkedType=&linkedId=` emits one `evidence.list_accessed` chain anchor per call with the linkedType + linkedId + row count (no per-row ids in the payload). A session-token theft that can't pass step-up still leaves an audit trail when bulk-walking metadata. Step-up on `/decrypt` is gated by `checkStepUpFreshness` at 60s; the action string `evidence.read` is reported in the challenge for the client's UX, not enforced as a per-action binding (see T-R29 / 1.9 sec-F1 close-out). | Metadata (GPS, capturedAt, uploadedAt, sha-prefix) remains visible to the authenticated principal; only the plaintext stays gated by step-up. Documented in `docs/runbooks/evidence.md`. |
| T-E14 | GET decrypt endpoint is exploitable by same-site phishing tabs that fire `<img src>` / `<iframe src>` | Per sec-F2 close-out: the decrypt handler requires `X-Requested-With: jhsc-web`. The web client sends it on every call; passive cross-origin element loaders cannot set custom headers. Returns 403 on missing header. SameSite=Strict on `__Host-access` is the primary defense; this is the belt-and-suspenders. The step-up gate at 60s freshness backstops this — the action string is reported in the WWW-Authenticate challenge for client UX, not enforced as a per-action binding (T-R29 / 1.9 sec-F1 close-out). | None material. |
| T-E15 | Inline PDF / HTML viewer in the reveal tab executes embedded script at blob: origin | Per sec-F6 close-out: decrypt response sets `Content-Disposition: attachment` (forces a download dialog instead of inline render) and `Content-Security-Policy: default-src 'none'; sandbox` (no network egress even if the viewer ignores the disposition). | Defense-in-depth; the bytes still exist in the user's downloads folder, which is the file-system's problem. |
| T-E16 | Client pins a new upload to a retired workplace key id, surviving rotation | Per sec-F5 close-out: finalize re-derives the active workplace key id from `getActiveWorkplacePublicKey()` at request time and rejects (422) when the client's claimed `workplaceKeyId` doesn't match. Stops a malicious or buggy client from sealing under a key id that may be compromised post-rotation. | Forward-seam against the 1.12 rotation script. None material today. |

**Audit hooks for evidence.** Three new event kinds in `packages/shared-types` `AuditEventKind`: `evidence.uploaded`, `evidence.read`, and `evidence.list_accessed`. Payloads stay PI-free; `plaintext_sha256` is the integrity anchor (one-way hash). `evidence.list_accessed` carries `{linkedType, linkedId, rowCount}` only — no per-row ids — and emits once per list call.

### 2.8 Inspection threats (Milestone 1.8)

Inspections land the first surface where a single domain object spans **template immutability** (CLAUDE.md #13), **two status vocabularies coexisting in production**, **CSA-copyrighted source material** (CLAUDE.md #5), **chain-anchored multi-party signatures**, and a **server-side PDF render that decrypts every linked photo for the duration of one request** (CLAUDE.md #16). The threat surface is the union of 1.5 (encrypted free text), 1.6 (manual promotion + polymorphic source FK), and 1.7 (workplace-key decrypt + step-up), plus three new surfaces: template versioning, three-signature workflow, and stored PDFs in Tigris. Every mitigation below cites the ADR-0007 subsection or the non-negotiable it implements.

| # | Threat | Mitigation | Residual |
|---|---|---|---|
| T-I1 | A future migration UPDATEs `inspection_templates.sections` and silently mutates how a historical inspection renders, breaking CLAUDE.md #13 | ADR-0007 §3.1: templates are append-only — editing inserts a new row with `version_number = max+1`; the prior row stays. No API surface UPDATEs `sections`. `inspections.template_version_id` FKs the specific versioned row (not `template_code`) with `ON DELETE RESTRICT`. The CI migration linter (1.6 sec-F1 shape) rejects any `UPDATE inspection_templates SET sections` diff. | A direct DBA-level SQL UPDATE bypasses the migration linter; detected by `audit-log-verify` because `audit.inspection_template.seeded` anchors carry the row's content hash. Acceptable. |
| T-I2 | Inspector pins a hand-crafted `template_version_id` to v2 mid-conduct via a forged PATCH, replacing the v1 pin (CLAUDE.md #13) | ADR-0007 §3.1: `inspections.template_version_id` is set at create time only. The PATCH route's Zod schema excludes `template_version_id` from `updatable`; the column is also written once in the create transaction. `ON UPDATE RESTRICT` on the FK is the DB-side backstop. | None material. |
| T-I3 | `inspections.template_version_id` ships nullable, allowing an inspection row with no template pin to slip through and re-resolve on read | ADR-0007 §3.1: column is `NOT NULL` with `ON DELETE RESTRICT`. Migration 0007 includes the constraint and a test asserts INSERT-without-template-version fails. | None material. |
| T-I4 | A custom template ships a `status_vocab` JSONB with no `promotable=false` markers, breaking the X/G fail-closed gate (CLAUDE.md #15) | ADR-0007 §3.2 + §3.5: the Zod schema for `status_vocab` requires `promotable: boolean` on every code; templates that omit it are rejected at the template POST. The promote route in §3.7 reads `status_vocab[status].promotable` and fail-closes if the key is missing (treats undefined as non-promotable). | None material. |
| T-I5 | Status vocab JSONB shape drift between template versions confuses the promotion route — a v2 template renames the `severityHint` field and the promote endpoint crashes mid-transaction | ADR-0007 §3.2: vocab shape is fixed by Zod at write time; reads against a stored vocab go through `parse()` not `as`, so a drifted row fails loud at the route layer with 500 + chain-anchored error event, never half-promotes. | A v0 row written before the Zod gate landed would crash forever; mitigated by the seeded-templates-only constraint in 1.8 (custom templates land via the same Zod path). |
| T-I6 | A workplace renames `ZONE_3_NAME` from "Receiving" to "Cold Warehouse" and historical Zone-3 inspections silently re-label, fabricating a record (CLAUDE.md #14) | ADR-0007 §3.3: `inspections.zone_id` stores the stable `zone_N` literal; display names render from `config/workplace.ts:resolveZoneLabel()` at view time. The historical row is unchanged; only the rendered label moves. Audit-chain payloads carry `zoneId` (stable), never the display name. | The PDF export footer renders the live display name as of export time, not the conduct-time name. Documented in the runbook; the chain anchor carries the stable id. |
| T-I7 | Zod refinement against `ZoneId` is missing on one route and `zone_99` lands in a row | ADR-0007 §3.3: validation is centralized in `packages/shared-types/src/inspection.ts`'s `inspectionZoneIdSchema` and imported by every inspection route. Integration test asserts each route rejects `zone_99`. The DB column is `text` with no FK by design (the `ZoneId` union is the single source of truth — same posture as `workplace_keys` in 1.7). | A future route that forgets to import the shared schema would slip; mitigated by the shared-types import lint rule. |
| T-I8 | A future PR transcribes verbatim CSA A344 text into the Rack template seed, violating CLAUDE.md #5 | ADR-0007 §3.4: seed stores only (a) clause numbers, (b) commonplace section headings, (c) original-language summaries we author. CI grep on `scripts/seed-inspection-templates.ts` flags PRs that introduce strings exceeding a length threshold (480 chars per criteria, hard-capped) or matching CSA's known phrasings (`csa-phrase-deny.txt` blocklist). Two-person review gate on any change to the seed (same pattern as legal-corpus priv-F3 close-out from 1.4). | An editorial slip past both reviewers and the grep; backstopped by the same 2-person fixture review that gates legal-corpus changes. |
| T-I9 | The seed runs at first-run, then a redeploy re-seeds and double-anchors the same templates | ADR-0007 §3.4: seeder is idempotent — `INSERT … ON CONFLICT (template_code, version_number) DO NOTHING`. The `audit.inspection_template.seeded` event is emitted only on actual INSERT (the seeder checks `RETURNING id`). A redeploy with no fixture change is a no-op against both the table and the chain. | None material. |
| T-I10 | The `audit.inspection_template.seeded` payload includes CSA-derived section text, leaking copyrighted material into the chain (CLAUDE.md #5) | Payload shape (ADR-0007 §3.11 + below): `{templateVersionId, templateCode, versionNumber, sectionCount, contentSha256}`. **Never** stores section text, item text, or criteria in the chain payload. The `contentSha256` is the integrity anchor; the canonical text lives in the row and is only readable by an authenticated rep at view time. Same typed-payload invariant as 1.3 priv-F2. | None material. |
| T-I11 | A future custom-template endpoint accepts user-supplied HTML/markdown in `sections` and stores stored-XSS that fires when the inspection conduct view renders | ADR-0007 §3.5: `sections` is JSONB validated by a Zod strict schema with no string field accepting `<` or `>` (same posture as legal-corpus sec-F1 close-out from 1.4). No HTML, no markdown — only `section_number`, `title`, `items[{item_number, text, criteria?}]`, `closing_section?`. Max sizes enforced (30 sections, 30 items, 240/480 chars). Web renderer treats every string as plain text via React's default escaping; no `dangerouslySetInnerHTML`. | A direct SQL INSERT bypasses Zod; detected by `audit-log-verify` because the chain anchor for a templated write carries the Zod-validated shape's hash. |
| T-I12 | `observation_ct` / `corrective_action_ct` ciphertext leaks via the inspection-findings list endpoint (mirror of T-H4 from 1.5) | ADR-0007 §3.6: list route projects `{id, item_number, item_text, status, has_observation: bool, has_corrective_action: bool, promoted_action_item_id}`. Plaintext requires the detail route + step-up (same posture as 1.5 hazards detail). Title text (`item_text`) is the template-snapshotted question — non-PI by construction (template content is non-PI per #5). | Step-up bound; an authenticated rep with their own factors can decrypt their own findings. Same scope as the rest of the encrypted surface. |
| T-I13 | A finding's free-text `observation` carries a co-worker's name, collected without consent (PIPEDA P3) | ADR-0007 §3.6: observation is envelope-encrypted at rest. The capture-flow placeholder copy guides toward non-PI summaries (same posture as 1.5 T-H5 hazards title field). The PIPEDA Principle 9 procedure in `docs/runbooks/inspections.md` (follow-up) covers redaction by re-encrypting with the named field nulled. | Rep discipline is the bound; documented. |
| T-I14 | The new `'inspection_finding'` `linkedType` is added to `apps/api/src/routes/evidence/index.ts` `acceptedLinkedTypes` but the `evidence_files_linked_fk_guard` trigger is not extended, letting a finding-id that doesn't exist FK through (mirror of 1.7 sec-F4 forward-seam ratchet) | ADR-0007 §3.6 + §3.11: 1.8 ratchets BOTH the route allow-list AND the trigger in the same migration (0007). Integration test asserts a forged `linkedType='inspection_finding' linkedId=<bogus uuid>` finalize call fails with 422 at the route, and a hand-crafted INSERT fails with 23514 at the trigger. The two ratchets land in the same migration to prevent half-rollout. | None material — the explicit ratchet pattern is the residual mitigation of T-E6 itself. |
| T-I15 | A hand-crafted POST to `/api/inspection-findings/:id/promote` skips the X/G fail-closed gate (CLAUDE.md #15) because the UI hides the button but the API doesn't enforce | ADR-0007 §3.7 step 2: route loads the pinned template's `status_vocab` and returns 422 `finding_not_promotable` if `status_vocab[finding.status].promotable === false`. The check runs **before** any DB write or transaction open. Integration test asserts a status-X finding rejects with 422 even when the UI is bypassed entirely (raw `curl`). | None material. |
| T-I16 | A finding is promoted twice via concurrent POSTs, creating two action_items pointing at the same source | ADR-0007 §3.7 step 3: route fail-closes if `promoted_action_item_id IS NOT NULL` with 422 `already_promoted`. The transaction acquires `SELECT … FOR UPDATE` on the finding row before the check, so two concurrent calls serialize and the loser sees the winner's `promoted_action_item_id`. | None material. |
| T-I17 | The bidirectional FK (`inspection_findings.promoted_action_item_id` ↔ `action_items.source_id`) drifts under concurrent edits — finding points at action item A, action item B points back at finding | ADR-0007 §3.7 step 4–5: both sides written in the same DB transaction; the action_items INSERT happens first (returning the new id), then the finding UPDATE pins it. `ON UPDATE RESTRICT ON DELETE RESTRICT` on both FKs. The action-items source guard trigger (1.6 sec-F7 ratchet pattern) ensures `source_id` references an existing finding. | None material. |
| T-I18 | The 1.6 `action_items_source_fk_guard` trigger doesn't know about `'inspection'` source_type until 1.8 ratchets it (the 1.6 priv-AI-F3 fail-closed forward seam) | ADR-0007 §3.7 step 7 + §3.11: migration 0007 extends the trigger with an `inspection` branch validating `source_id IN (SELECT id FROM inspection_findings)`. The action-items route's `sourceType` Zod refinement adds `'inspection'` in the same slice (step 8). Integration test asserts a `POST /api/action-items` with `sourceType='inspection'` and a non-existent finding id fails with 23514 at the trigger. Both ratchets land in migration 0007. | None material — explicit ratchet closes the 1.6 forward seam. |
| T-I19 | A signature row is written without a chain anchor, breaking the per-signature audit guarantee | ADR-0007 §3.8: `inspection_signatures.audit_idx` is `NOT NULL` with FK to `audit_log(idx)`. The route writes the audit append + the signature row in one DB transaction; failure on either rolls back both (same shape as 1.5 T-H9). | None material. |
| T-I20 | A signature is attributed to the wrong role — the Inspector is recorded as the JHSC Worker Co-Chair | ADR-0007 §3.8: route requires `signed_by_user_id = auth.userId` for the inspector + co-chair roles (no signing-on-behalf). The `(inspection_id, signature_role)` UNIQUE index prevents a single user from claiming two roles on one inspection; a separate role-membership check (against the user's `role` column) gates the co-chair role specifically. | Role rotation: signatures stored against `user_id` pin to the user at signing time, not to "whoever holds the co-chair role today." This is the correct shape for arbitration but means a re-org doesn't relabel historical signatures — call out in runbook. |
| T-I21 | The "three signatures required before rack export" gate is enforced only client-side; a hand-crafted `POST /api/exports/inspection` slips a not-yet-fully-signed rack inspection through | ADR-0007 §3.8 + §3.9: export route loads the inspection's pinned template's `requires_signatures` array, queries `inspection_signatures` for the matching roles, fail-closes with 422 `signatures_incomplete` if any required role is missing. The check runs before the workplace private key is opened (so a forged call never even touches plaintext). | None material. |
| T-I22 | `inspection_signatures.note_ct` leaks via the signatures list endpoint | ADR-0007 §3.8: list route projects `{role, signedByUserId, signedAt, has_note: bool}`; ciphertext requires the detail route + step-up. Same shape as the findings list (T-I12). | Same step-up bound. |
| T-I23 | Plaintext photo buffer persists in API memory past the PDF response (mirror of 1.7 sec-F9 plaintext-lifetime risk, made worse by per-PDF batch render) | ADR-0007 §3.9 step 3: per-photo `sodium.memzero` is called the moment `pdfkit.image()` returns. The decrypt + embed + zero ordering is tested by spy-counting `sodium.memzero` calls against the linked-photo count (ADR-0007 §3.11 S4 plaintext-zero assertion). Workplace private key is `memzero`'d at request end (step 8). | Bun's GC may copy the buffer before `memzero` fires; mitigated by using `sodium_malloc`-backed buffers from `@jhsc/crypto` (same shape as 1.7 evidence decrypt). |
| T-I24 | One photo fails to decrypt mid-render, leaving an incomplete PDF that nevertheless gets a success chain anchor and a partial-plaintext leak | ADR-0007 §3.9: per-photo decrypt verifies `plaintext_sha256` against the `evidence_files` row before embedding (same integrity check as 1.7). On verify failure the entire render transaction aborts, the partial PDF bytes are discarded (in-memory only; never reached Tigris), and the chain anchor is **not** emitted (anchor + Tigris upload happen at step 5–6, after a fully successful render). The failed-render path emits a separate `inspection.export_failed` audit event (no payload PI) for diagnosability. | A photo whose ciphertext is corrupted blocks the export entirely; runbook covers the operator path (unlink the photo, retry). |
| T-I25 | The PDF is rendered with embedded JavaScript that ships out the chain hash when opened | ADR-0007 §3.9: `pdfkit` is configured with no JS-action support — the library does not expose a JS-injection API. Golden-PDF fixture tests parse the output with a JS-aware PDF reader and assert zero `/JS`, `/JavaScript`, `/AA`, `/OpenAction` entries. | Library bug surface; mitigated by the golden-PDF test running on every PR that touches `pdf.ts`. |
| T-I26 | Stored PDFs in Tigris are accessible without the `export_records` anchor — an attacker who guesses an `exportId` fetches the PDF directly | ADR-0007 §3.9: storage key is `exports/<uuid>/inspection-<uuid>.pdf` where the uuid is the unguessable `export_records.id`. Tigris bucket has no public-read policy; access requires presigned URLs minted by the API. The `GET /api/exports/:id` route requires step-up (same posture as the create route) and emits an `evidence.list_accessed`-shape audit anchor on each re-fetch (priv-F5 close-out pattern from 1.7). | UUID guessing is bounded by the 122-bit entropy of `gen_random_uuid()`. Acceptable. |
| T-I27 | The `output_sha256` anchored in the chain doesn't match the bytes returned to the client — TOCTOU on the PDF bytes between Tigris upload and client stream | ADR-0007 §3.9 step 7: route streams the bytes from the **same in-memory buffer** that was hashed and uploaded — no re-fetch from Tigris, no second decryption. The footer hash post-replace (the `__HASH__` two-pass dance in §3.9) recomputes the canonical hash AFTER replacement; the pre-replace hash is discarded. Golden-PDF test asserts footer hash == bytes hash on every PR. | The footer-hash post-replace logic is fiddly (ADR-0007 Risks); CI test is the bound. |
| T-I28 | `pdfkit`'s font subset or PDF metadata leaks workplace name into the document despite CLAUDE.md #1 (no specific names in source) | ADR-0007 §3.9: workplace name comes from `config/workplace.ts` at runtime (env-driven). The PDF metadata `/Author`, `/Title`, `/Producer` fields are set from runtime config, never hard-coded. Source Serif 4 TTF is shipped without name-table customizations. Golden-PDF test asserts `/Author` matches the runtime config and is non-empty. | Misconfigured env (workplace name = "TEST") would emit "TEST" into the PDF metadata; documented as a deployment-checklist item. |
| T-I29 | A batch export of 100 inspections silently truncates at 99 with no error | ADR-0007 §3.10: Zod schema enforces `inspection_ids.length in [1, 100]` inclusive; the route iterates the full array and includes a post-loop assertion that `processedCount === inspection_ids.length`, throwing `BatchExportTruncated` if not. The `inspection.exported` chain anchor carries `{inspectionIds, byteSize}` so a post-hoc audit catches a count mismatch. | None material. |
| T-I30 | The 60s step-up freshness floor is bypassed via a stale TOTP grant carried forward from an unrelated action (mirror of 1.7 export step-up posture, CLAUDE.md #16) | ADR-0007 §3.10: export routes call `checkStepUpFreshness({ maxAgeSeconds: 60, action: 'inspection.export' })`. **1.9 sec-F1 honest stance:** the freshness check binds to the elapsed time since the factor verification AND the actor identity; it does NOT bind to the action string (the action string is reported in the WWW-Authenticate challenge header for the client's UX, NOT enforced as a per-action binding on the server). A fresh grant obtained for any prior step-up action within the 60s window IS accepted here. True per-action binding is a 1.12 hardening item (T-R29 / runbook §11). `WWW-Authenticate` header carries `max_age="60"` so the web modal warns correctly (1.5 sec-F3 pattern). | Coarse-grained freshness; 1.12 closes the structural gap. |
| T-I31 | The 5/hour rate limit isn't enforced per-actor — a compromised account hammers exports because the bucket is per-IP | ADR-0007 §3.10: rate limit keys on `auth.userId`, not IP (export routes are authenticated). 5 exports/hour/user. Token-bucket middleware reused from §4a control 5. The bucket is in-memory per-process — the per-machine variant cap is N × 5/hour where N is the Fly Machine count. pg-boss-backed cross-process limiter is a 1.12 follow-up (recommendations runbook §11 mirrors). | A compromised account is still rate-limited; the chain anchor reveals the exfil pattern within minutes. |
| T-I32 | The 100-batch ceiling is Zod-enforced at the route but a hand-crafted INSERT smuggles 1000 IDs into `export_records.inspection_ids` | ADR-0007 §3.9: DB CHECK on `export_records` asserts `cardinality(inspection_ids) BETWEEN 1 AND 100`. The Zod schema + the DB CHECK are belt-and-suspenders. | None material. |
| T-I33 | The seed migration runs out of order on a redeploy and the `audit.inspection_template.seeded` anchor refers to a row that wasn't actually written | ADR-0007 §3.4 + §3.11: seeder is idempotent (T-I9) and runs **after** migration apply in the deploy sequence (documented in the inspections runbook). The seeder wraps each template's INSERT + audit append in one transaction; a failed INSERT means no anchor. `audit-log-verify --check-inspection-templates` cross-checks every `audit.inspection_template.seeded` anchor's `contentSha256` against the live row at nightly verify. | None material. |
| T-I34 | A template version migration (v1 → v2 of "Zone Monthly") re-renders historical v1 inspections under v2's questions because the chain anchor refers to `template_code` instead of `template_version_id` | ADR-0007 §3.1: chain payloads carry `templateVersionId` (the specific versioned row id), not `templateCode`. Both the row-level FK and the audit payload pin the version. The web detail view loads the inspection's findings against `template_version_id`, never re-resolving `template_code → latest`. Integration test asserts a v1 inspection rendered after v2 lands shows v1's questions verbatim. | None material — this is the structural enforcement of CLAUDE.md #13. |
| T-I35 | The export route admits non-`complete` inspections — `POST /api/inspections/exports` for a `scheduled` / `in_progress` / `awaiting_signatures` inspection slips a chain-anchored "JHSC Inspection Export" PDF for work that was never actually conducted or signed (S5 sec-F1 close-out) | `apps/api/src/routes/inspections/exports.ts` `assertInspectionExportReady` runs BEFORE `resolveInspection` opens the workplace private key and BEFORE any decrypt. Asserts `inspection.state = 'complete'` AND (for rack templates with `requires_three_signatures = true`) all three required roles (`inspector`, `supervisor`, `jhsc_worker_co_chair`) are present in `inspection_signatures`. Returns 422 `inspection_not_complete` / `signatures_incomplete` with the missing roles enumerated. Integration test asserts each of scheduled / in_progress / awaiting_signatures rejects with 422 (with step-up granted). | None material — gate runs server-side regardless of UI state. |
| T-I36 | `POST /api/action-items` with `sourceType='inspection'` opens a back-channel for inspection-source rows, bypassing both the #15 X/G fail-closed gate AND the T-I16 single-promote invariant (S5 sec-F2 close-out) | `apps/api/src/routes/action-items/index.ts` create-body Zod refinement drops `'inspection'` from the `sourceType` allow-list. POST returns 400 `inspection_source_requires_promote_route` so a hand-crafted caller gets a clear redirect to the dedicated `POST /api/inspections/findings/:id/promote` handler — the only legitimate emitter. The promote handler retains the X/G gate (T-I15), the FOR UPDATE row lock (T-I16), and the action-item INSERT. The trigger's `'inspection'` branch (sec-F4 close-out, T-I38) still validates the FK for the legitimate path. Integration test asserts a `POST /api/action-items` with `sourceType='inspection'` returns 400 with the redirect message. | None material — single legitimate path. |
| T-I37 | The promote handler welds decrypted observation + corrective_action plaintext into the action-item description, leaking PI via the no-step-up `GET /api/action-items/:id` route (S5 sec-F3 close-out) | `apps/api/src/routes/inspections/index.ts` promote handler derives a NON-PI description: `"Promoted from inspection finding: <section_label> / <item_label>. Open the finding for full context."` — no observation, no corrective_action, no responsible_party plaintext welded in. Section/item labels are template-snapshotted (non-PI per T-I12 / ADR-0007 §3.5). Anyone wanting finding text takes the step-up gated `GET /api/inspections/findings/:id` route (T-I30). The step-up scope-creep bypass closes. Integration test asserts the promoted action_item's `description` field does NOT contain the original observation text. | None material — the PI surface narrows; runbook §4 documents the no-finding-decrypt residual (priv-F3). |
| T-I38 | `action_items_source_fk_guard` fails-open for unsupported `source_type` values (recommendation, incident, future kinds) — a hand-crafted INSERT writes a row with a dangling source pointer that nothing validates (S5 sec-F4 close-out) | `migrations/0007_inspections.sql` rewrites the trigger with a fail-closed `ELSE RAISE EXCEPTION` rail. The function special-cases `NULL` / `manual` / `excel_import` at the top (referential-integrity-free by design). `'hazard'` and `'inspection'` branches validate the FK target. Any other `source_type` raises `action_items_source_fk_guard: source_type % not yet supported at trigger layer`. Mirrors the `evidence_files_linked_fk_guard` shape from the 1.7 sec-F4 close-out (T-I14). | None material — DB-layer backstop closes the route-Zod-only gap. |
| T-I39 | `PATCH /api/inspections/findings/:id` allows mutation of `statusValue` / `observation` / `corrective_action` after the finding has been promoted, letting a rep promote-then-cover-tracks (promote an A finding, mutate to X so the inspection record reads "no issue" while the open INSP action item stands) (S5 sec-F5 close-out) | `apps/api/src/routes/inspections/index.ts` PATCH handler, after the `FOR UPDATE SELECT` that already reads `promoted_action_item_id`, rejects with 422 `finding_immutable_after_promote` if the body changes any of `statusValue` / `observation` / `correctiveAction` while `promoted_action_item_id IS NOT NULL`. Editing `responsibleParty` only remains allowed (the field most likely to need amendment without changing the finding's substance). Integration test asserts a promoted finding rejects a status-flip PATCH with 422. | None material — the promote anchor + the action_item's own chain are the durable records; the finding's substance is pinned. |
| T-I40 | Stored PDFs are written to Tigris without `ServerSideEncryption` — ADR-0007 §3.9 pledged "Tigris-side encryption (SSE)" but `putEvidenceObject` set no SSE parameter on the PUT command, leaving rendered PDFs (finding observations, corrective actions, photo plaintext) at rest in cleartext (S5 priv-F1 close-out) | `apps/api/src/evidence/tigris.ts` `buildPutObjectCommand` sets `ServerSideEncryption: 'AES256'` on every PutObjectCommand. `AES256` is the safe S3-compatible default; `aws:kms` is a future swap when a KMS key is provisioned (runbook §8 flags as ops follow-up). Unit test asserts the parameter is set. | Bucket-level default-encryption policy + Tigris lifecycle policy for `exports/*` are ops follow-ups (runbook §8 + 1.12). The route's `expires_at` is the only TTL enforcer until then. |
| T-I41 | `POST /api/inspections/:id/findings` accepts `awaiting_signatures` as an allowed state — once the inspector signs a rack inspection, the rep can still create findings while the supervisor and co-chair are reading (S5 sec-F7 close-out) | `apps/api/src/routes/inspections/index.ts` POST findings handler restricts the state allow-list to `['scheduled', 'in_progress']` only. Late-added findings after the inspector signs are blocked with 422 `inspection_not_open_for_findings`. The symmetry with PATCH (`in_progress` only) makes chain-of-custody coherent: all finding authoring precedes the first signature. Integration test asserts POST in `awaiting_signatures` returns 422. | For zone_monthly the inspector signature transitions directly to `complete`, so this path was always closed; the fix matters for rack. |
| T-I42 | Tigris PUT happens BEFORE the chain anchor + DB INSERT — a transaction failure after PUT leaves an orphan PDF in Tigris with no `export_records` row pointing at it and no chain anchor (S5 sec-F6 close-out) | `apps/api/src/routes/inspections/exports.ts` wraps the `db.transaction` call in a try/catch. On rollback, best-effort `DeleteObject` against the orphaned `storageKey` via `deleteEvidenceObject(storageKey)`. Logged at warn level (success or failure). Never throws from cleanup — the original transaction error re-raises. The `expires_at` cleanup remains the primary TTL bound; this catch closes the chain-mismatch window. | A best-effort delete can fail (NoSuchKey, network drop); the runbook §8 covers the bucket lifecycle policy follow-up for any orphans that survive both gates. |
| T-I43 | The PDF photo captions render GPS coordinates without per-export consent or redaction option — every exported PDF distributes ~11m-resolution coordinates whether the rep meant to or not (S5 priv-F7 close-out) | `apps/api/src/routes/inspections/exports.ts` create-body Zod schema gains `includeGps: z.boolean().default(false)`. `renderInspectionPdf(..., { includeGps })` only renders the `GPS lat, lon` caption fragment when `includeGps === true`. The default flipped to opt-out: GPS is NOT in the PDF unless the rep affirmatively checks the export panel's "Include GPS coordinates" checkbox. Mirrors the PIPEDA P4 limiting-collection posture for distributable artifacts. Integration test asserts a default-false export's PDF carries no `GPS ` prefix; a true export carries it. | The 1.7 T-E5 GPS-resolution cap (~11m) remains; the export gate is layered on top. |
| T-I44 | The PDF renderer hard-ignores the workplace's configured zone display names — surfaces stale labels ("Zone 3" instead of "Cold Warehouse") in the rep's evidentiary exhibit (S5 priv-F6 close-out) | `apps/api/src/routes/inspections/exports.ts` `resolveZoneDisplayName` reads from `loadWorkplaceConfig().zones`. Returns the configured `displayName` if non-empty AND ≤120 chars AND contains no `<` / `>` (HTML strip). Falls back to `defaultName` ("Zone N") silently on violation — no crash, no warning. The metadata fields (`/Title`, `/Author`, `/Producer`) stay generic per T-I28; only the rendered text body surfaces the configured zone label. Workplace name itself stays out of the PDF body until 1.9 (runbook §11). | An over-length or HTML-containing `ZONE_N_NAME` env value silently falls back to the default — deployment-checklist item; runbook §10. |

**Audit hooks for inspections.** Six new event kinds added to `packages/shared-types` `AuditEventKind` (ADR-0007 §3.11). All payloads pass through the same typed-emit boundary as 1.3 priv-F2 — PI-clean by construction (enum IDs + hashes + counts only, never names or free text):

- `inspection.created` — `{inspectionId, templateVersionId, zoneId, conductedByUserId}`. No template content, no zone display name.
- `inspection_finding.created` — `{findingId, inspectionId, itemNumber, status}`. No `observation`, no `corrective_action`, no `responsible_party`.
- `inspection_finding.promoted` — `{findingId, actionItemId, risk}`. No description, no owner. Pairs with the action-items side's own `action_item.created` anchor (both land in one transaction per §3.7 step 6).
- `inspection.signed` — `{inspectionId, role, signerUserId}`. No `note_ct`. Each signature emits its own anchor (per §3.8: separate-row design over JSONB blob).
- `inspection.exported` — `{exportId, inspectionIds, outputSha256, byteSize}`. The `outputSha256` is the one-way integrity anchor; `inspectionIds` is opaque uuid[].
- `audit.inspection_template.seeded` — `{templateVersionId, templateCode, versionNumber, sectionCount, contentSha256}`. **Never** stores section text, item text, or CSA-derived criteria (T-I10).

A seventh ratchet event — the extension of `evidence_files.linked_type` to accept `'inspection_finding'` — is **not** a new chain kind but a forward-seam ratchet on the existing `evidence.uploaded` / `evidence.read` / `evidence.list_accessed` events from 1.7. The 1.8 migration extends the `evidence_files_linked_fk_guard` trigger and the route's `acceptedLinkedTypes` array in lockstep (T-I14); the audit payload shape from 1.7 already accommodates the new linkedType value without schema change.

**Documented residuals on the inspections audit surface (S5 priv-F3 / priv-F5).** The six-kind contract leaves two read-path anchors unimplemented in 1.8, both deferred to 1.9 and documented in `docs/runbooks/inspections.md` §4:

- **No `inspection_finding.read` anchor.** `GET /api/inspections/findings/:id` decrypts observation / corrective_action / responsible_party and returns plaintext. The route is step-up gated at 60s, but no chain anchor fires on success. An attacker who walks every finding by id leaves no per-read trail (the `inspection.created` + `inspection_finding.created` anchors bound the surface). 1.9 adds the kind without ceremony (the 1.7 evidence path added three kinds the same way).
- **No `inspection.export.downloaded` anchor.** `GET /api/inspections/exports/:id/download` is step-up gated + CSRF-header gated, but re-downloads after the original `inspection.exported` anchor are invisible to the chain. The 30-day TTL + per-request step-up freshness are the residual bound; the inline comment in `exports.ts` commits to the 1.9 ratchet.

### 2.9 Recommendation threats (Milestone 1.9)

Recommendations are the worker co-chair's most consequential statutory artifact (OHSA s.9(20) / CLC s.135(5)) and the first surface to combine **long-form encrypted prose** (200–2000 word body with inline citation markers), a **status machine that spans an external counterparty** (the 21-day s.9(21) clock waits on management's response), a **cryptographically signed PDF** under a workplace-controlled Ed25519 key, and a **chain-of-custody guarantee that survives withdrawal**. The threat surface is the union of 1.5 (encrypted free text + status machine), 1.6 (action-item bridge + polymorphic source FK), 1.7 (workplace-key decrypt + step-up + Tigris SSE), and 1.8 (PDF render + step-up gate + 30-day TTL + signature workflow), plus four new surfaces: per-jurisdiction recommendation numbering, the `[[cite:N]]` marker discipline, the signed-export ZIP bundle, and the second workplace key (Ed25519 signing, separate primitive from the X25519 sealed-box encryption key). Every mitigation below cites the ADR-0008 subsection or the CLAUDE.md non-negotiable it implements.

| # | Threat | Mitigation | Residual |
|---|---|---|---|
| T-R1 | A hand-crafted PATCH to `/api/recommendations/:id` flips `status` from `draft` to `resolved` directly, bypassing the submit + response transitions and silently skipping the chain anchors that prove what management actually saw | ADR-0008 §3.1: each lifecycle transition is its own dedicated endpoint (`/submit`, `/responses`, `/resolve`, `/withdraw`); the general PATCH route's Zod schema excludes `status` from the allow-list and applies `.strict()` on top — a PATCH with `status='resolved'` reaches the `.strict()` reject and returns **400 `invalid_body`** (NOT 422 `status_not_updatable` — S5 priv-F6 close-out corrects the prior doc/code drift; the route-layer Zod `.strict()` is the gate). The transition graph is enforced at the route layer per ADR-0007 §3.7 fail-closed posture — the DB CHECK accepts every value in `recommendationStatus` because the API is the single source of truth. Integration test asserts a hand-crafted PATCH with `status='resolved'` returns 400 `invalid_body`. Non-negotiable #2 — every transition is a chain anchor. | None material. The DB CHECK breadth is the documented tradeoff (matches §3.1's "API surface is the single source of truth for transitions"). |
| T-R2 | A withdrawn recommendation is resubmitted via a hand-crafted POST to `/submit`, producing a second `recommendation.submitted` chain anchor and a second linked action item for what is evidentially the same recommendation | ADR-0008 §3.1: `withdrawn` is terminal alongside `resolved`; the `/submit` handler asserts `status='draft'` before any state change. Integration test asserts `/submit` against a withdrawn (or already-submitted) recommendation rejects with 422 `submit_requires_draft`. The withdrawal itself is the chain-of-custody evidence per non-negotiable #2 — there is no DELETE path on withdrawn rows. | A rep who genuinely needs to re-issue a withdrawn recommendation creates a new recommendation; the runbook covers the `link_kind='replaces'` (deferred) seam. |
| T-R3 | Zod `recommendationStatus` enum drifts from the SQL CHECK — adding a new state (e.g. `under_review`) in code without the migration leaves the DB CHECK rejecting writes the route now produces | ADR-0008 §3.2: the enum lives in `packages/shared-types/src/index.ts` and is the single source for both the Zod schema and the migration's CHECK literal. The migration linter (1.6 sec-F1 shape) flags any drift between the shared-types enum and the live CHECK in migration 0008. Tests assert every Zod-accepted value is in the live CHECK. | A future enum addition that skips updating the migration linter manifest could drift; mitigated by the lint rule + CI grep on the CHECK literal. |
| T-R4 | Two reps concurrently call `/submit` on different drafts in the same jurisdiction and both receive `recommendation_number=14`, breaking the `(jurisdiction, recommendation_number)` UNIQUE | ADR-0008 §3.2: per-period sequence allocated via `pg_advisory_xact_lock(hashtext('recommendations.number.<jurisdiction>'))` inside the submit transaction — same `allocateSequenceNumber` shape as `apps/api/src/routes/action-items/index.ts:945`. Two concurrent submits serialize; the second sees `MAX(recommendation_number) WHERE jurisdiction=$1` already advanced. The UNIQUE index is the structural backstop — a missed lock surfaces as a 23505 rollback, not a duplicate row. | Single-co-chair scope makes contention negligible; the advisory-lock pattern is proven on action-items 1.6. |
| T-R5 | The list endpoint projects `title_ct` / `body_ct` plaintext, leaking PI-bearing recommendation text (supervisor names, accommodation context, reprisal narratives) bypassing the step-up gate that the detail route enforces (mirror of T-H4 / T-I12) | ADR-0008 §3.2: list route projects `{id, recommendation_number, jurisdiction, status, drafted_at, submitted_at, response_received_at, resolved_at, withdrawn_at, drafted_by_user_id, citation_count, response_count, deadline_at, days_remaining}` only. `title_ct` / `body_ct` decrypt requires the detail route + step-up (same posture as 1.5 hazards detail + 1.8 findings detail). Title is not exposed at all in list; the rep navigates by recommendation number + status. Non-negotiable #4. | Step-up bound; an authenticated rep with their own factors can decrypt their own recommendations. Same scope as the rest of the encrypted surface. |
| T-R6 | A rep types a `[[cite:5]]` marker in the body with no corresponding citation list entry, producing a dangling marker that the renderer expands to an empty footnote in the signed PDF | ADR-0008 §3.3: submit-handler Zod refinement asserts every `[[cite:N]]` marker in the decrypted body has a `position=N` entry in the citation list AND vice versa AND positions are dense 1..K with no gaps. Submit rejects with 422 `citation_marker_density_violation` enumerating the offending positions. Client-side CitationRef component runs the same Zod rules pre-submit (§3.13 S3) so the rep sees the error inline. Non-negotiable #5. | A hand-crafted SQL INSERT bypasses Zod; detected because the renderer's expand-pass fails loud (no silent footnote omission) and the export route's `assertCitationsResolvable` rejects before plaintext is opened. |
| T-R7 | A `recommendation_citations` row references a `(statute_code, citation_text, version_date)` triple that does not exist in `legal_clauses`, violating non-negotiable #5 — a fabricated OHSA citation slips through | ADR-0008 §3.3: submit-handler resolves every triple against `statutes JOIN clauses` at submit time and rejects with 422 `citation_not_in_corpus` if any has no row. The `clause_id` column FKs `clauses(id)` with `ON DELETE RESTRICT`; the citation row cannot exist without a corpus row. Additionally the resolver asserts the joined clause's `corpus_version` matches the active `corpus_versions.version` (rejects citations against retired corpus versions per §3.3). CI test in `packages/legal-corpus` asserts the validator rejects a known-bad triple. Non-negotiable #5. | A direct INSERT that races a corpus migration (citation valid at INSERT, retired before submit chain) — bounded by `ON DELETE RESTRICT` on `clauses` and the seeder's INSERT-only / never-DELETE discipline from 1.4 T-LC5. |
| T-R8 | The corpus is amended (`version_date` V → V') after a recommendation is submitted; the signed PDF's footnote provenance hashes verify against the old V but a reader fetching the live corpus sees V', generating an "evidence tampered" false alarm | ADR-0008 §3.3 + §3.8: `recommendation_citations` snapshots `citation_text`, `version_date`, and `body_hash` at submit time — the citation row carries the corpus state pinned to the recommendation, not a live-resolving pointer. The signed PDF's footnote line reads `[N] {citationText} ({versionDate}) — {bodyText}. Provenance: clause hash {bodyHash}` — the verifier checks against the historical clause row by `version_date`, which the 1.4 corpus's append-only-version invariant (T-LC3 / T-LC5) guarantees still exists. The chain anchor's `citationsHash` (§3.8) is sha256 of the canonical-JSON of the resolved citations including each `body_hash` — a re-export of the same recommendation after a corpus amendment produces a different `citationsHash` and the chain records both. Non-negotiable #5. | The runbook documents the recipient verification recipe: fetch the historical clause row via `(statute_code, citation, version_date)` rather than the live active version. |
| T-R9 | A response row at `position=N` is UPDATEd to alter the management text after the fact — the evidentiary append-only contract is broken silently | ADR-0008 §3.4: `recommendation_responses` has no API write path beyond `POST /api/recommendations/:id/responses` (an INSERT-only handler); the general PATCH route excludes response columns entirely. The `(recommendation_id, position)` UNIQUE plus the `position >= 1` CHECK make a clobber require explicit UPDATE SQL — detected at audit-chain verify (the `recommendation.response_captured` anchor carries `responseId + position` only, so an out-of-band UPDATE leaves the chain anchor pointing at the wrong text). Non-negotiable #2 — every response is its own chain anchor. | Detection-only at the DB level; runbook tamper procedure covers the response. A typo in transcription is corrected by appending `position=N+1`, never edit (§3.4 + accepted tradeoff). |
| T-R10 | Two reps concurrently POST responses for the same recommendation; both compute `position=2` from the same MAX query and the UNIQUE collides at the second insert, producing a 500 instead of an ordered append | ADR-0008 §3.4: response insert acquires `SELECT MAX(position) FROM recommendation_responses WHERE recommendation_id=$1 FOR UPDATE` (or `pg_advisory_xact_lock(hashtext('rec.response.' || rec_id))`) inside the transaction. Two concurrent posts serialize; the second sees the first's position. The UNIQUE is the structural backstop. Integration test asserts concurrent inserts produce position 1 then 2, never a 23505. | Same per-row lock posture as 1.5 T-H8 / 1.6 T-AI5. |
| T-R11 | The response `body_ct` / `author_role_ct` plaintext is returned in the list projection, leaking management strategy + counterparty role | ADR-0008 §3.4: list route projects `{position, received_at, received_by_user_id}` per response; body + author_role decrypt requires the detail route + step-up. The status flag `has_response: bool` exposes presence without text. The chain payload carries `responseId + position + recommendationId` only — no role text, no body. Non-negotiable #4. | Step-up bound; same scope as T-R5. |
| T-R12 | The action-item bridge is auto-created at draft instead of submit, cluttering the minutes' `recommendation` section with abandoned drafts and leaking the draft's existence into the chain before the rep has committed to submit | ADR-0008 §3.5: action item creation is wired only into the `/submit` handler; the draft create handler emits `recommendation.created` (no action_item) and writes nothing to `action_items` or `recommendation_action_item_links`. Integration test asserts `POST /api/recommendations` followed by `GET /api/action-items?section=recommendation` does not show the draft. Non-negotiable #12 — action items are first-class, not draft byproducts. | None material. |
| T-R13 | The submit transaction creates the recommendation row + the action_item row + the link row + the chain anchor; a mid-transaction failure (advisory-lock contention, audit_log_pkey collision) leaves orphans: action_item with no link, or link with no action_item | ADR-0008 §3.5: all four writes — recommendation status flip, action_items INSERT, recommendation_action_item_links INSERT, `recommendation.submitted` audit append — run inside one `db.transaction`. Failure on any one rolls back all four; the recommendation stays in `draft`, no orphan action_item, no orphan link. Same posture as 1.5 T-H9 + 1.8 T-I17 bidirectional FK transaction. Non-negotiable #2. | Verified by integration test (§3.13 S2). The rep retries — the chain row is the proof of what actually completed. |
| T-R14 | The 1.6 `action_items_source_fk_guard` trigger is not ratcheted with a `'recommendation'` branch in migration 0008; a `POST /api/action-items` with `sourceType='recommendation'` and a bogus `source_id` slips a dangling action item through (1.6 priv-AI-F3 / 1.7 sec-F4 / 1.8 T-I38 forward-seam pattern) | ADR-0008 §3.5 + §3.13: migration 0008 extends `action_items_source_fk_guard` with a `'recommendation'` branch validating `source_id IN (SELECT id FROM recommendations)`. The trigger's `ELSE RAISE EXCEPTION` rail (1.8 T-I38 / sec-F4 close-out) preserves the fail-closed posture for future kinds. The action-items route's `sourceType` Zod refinement is widened in lockstep. Both ratchets land in the same migration to prevent half-rollout. Integration test asserts `POST /api/action-items` with `sourceType='recommendation'` and a bogus uuid fails with 23514 at the trigger. Non-negotiable #12. | None material — explicit ratchet closes the 1.6 forward seam. |
| T-R15 | A withdrawn recommendation leaves its linked action_item in an active section (`recommendation` or `old_business`) — the minutes show the action item as live even though the recommendation behind it is dead | ADR-0008 §3.5: the `/withdraw` handler invokes the 1.6 section-move handler inside its transaction to move the linked action_item to `section='archived'` with `status='Cancelled'` and the literal template-supplied reason `"Recommendation withdrawn."` (NOT the rep's free-text reason — that lives only on `recommendations.withdrawal_reason_ct`). Two chain rows (`action_item.moved` + `recommendation.withdrawn`) land in one transaction. Integration test asserts the move happens and the action_item is unfindable in `section='recommendation'` post-withdrawal. | None material. |
| T-R16 | The 21-day s.9(21) deadline is computed only client-side; a rep can render an ON recommendation as "3 days remaining" past day 21 by manipulating the local clock, undermining the evidentiary surface that "responded on day N" is supposed to be | ADR-0008 §3.6: the `recommendation-clock.ts` pure function runs server-side in the projection (same posture as 1.6 action-flag.ts per T-AI7). The web client renders the returned `deadline_at` + `days_remaining` + badge severity verbatim and does not re-compute. Integration test asserts list/detail projections include the server-computed clock fields. Status flipped to `response_received` pins `days_to_respond = received_at - submitted_at` server-side; the badge surfaces that as "responded on day N" — the deficiency stays evidentially recorded even if management responded late. Non-negotiable #7 (rights-protective UI). | Client cache staleness across day boundaries (mirror of T-AI7 residual); a list cached at 23:59 reads "3 days" then morning is "2 days." The rep refreshes. |
| T-R17 | A CA-FED recommendation displays a fixed 21-day or 30-day deadline despite CLC s.135(6) being "as soon as possible" — the app invents a statutory deadline that doesn't exist (non-negotiable #5 forbids invention) | ADR-0008 §3.6: the `recommendation-clock.ts` projection branches on `jurisdiction` — CA-FED returns `{days_since_submission: N, deadline_at: null, badge_severity: 'informational'}` with no color escalation. The UI badge reads "days since submission: N" with no overdue label. No default 30-day backstop. Tests assert CA-FED projections never include a non-null `deadline_at`. Non-negotiable #5 / #7. | A rep operating in a hybrid jurisdiction (rare; mostly federal aviation/marine/banking) reads the badge carefully; runbook covers the distinction. |
| T-R18 | The signed PDF carries the signature bytes but not the `signing_key_id` — a verifier confronted with two workplace keys (post-rotation) doesn't know which public key to verify against | ADR-0008 §3.7 + §3.9: the ZIP bundle's `manifest.json` carries `signing_key_id`, `signing_key_fingerprint`, and `signature_algorithm`. The `GET /api/recommendations/exports/:id/manifest` endpoint returns the manifest + the historical public key for that id (retired keys stay queryable per §3.7). Verifier reads the fingerprint, fetches the matching public key, runs `sodium.crypto_sign_verify_detached`. The chain anchor's `signing_key_id` (in `recommendation.exported` payload) lets `audit-log-verify` cross-check that the manifest's claimed key id matches the chain row. Non-negotiable #16. | A future PAdES upgrade (§3.9 deferred to 1.12) reduces this to a single PDF the reader verifies natively; sidecar exports remain verifiable via the manifest route. |
| T-R19 | A migration bug or hand-crafted INSERT produces two `workplace_signing_keys` rows with `active=true`, leaving the export handler with a non-deterministic choice and risking signatures under the wrong key | ADR-0008 §3.7: partial UNIQUE index `workplace_signing_keys_active_unique ON (active) WHERE active = true` enforces "at most one active" at the DB layer. A second `active=true` INSERT fails with 23505. The seed runbook calls `getActiveSigningKey()` which returns the single row or throws — never silently picks one. | None material — DB-layer backstop, same shape as the 1.7 workplace_keys discipline. |
| T-R20 | The Ed25519 private signing key is cached in API memory past the export response (mirror of 1.8 T-I23 plaintext-lifetime), risking accidental sign-with-stale-key on the next request after a rotation | ADR-0008 §3.7 + §3.8: the private signing key is opened inside the export route's bounded-plaintext window using `sodium_malloc`-backed buffers from `@jhsc/crypto`, used to produce the `crypto_sign_detached` bytes, then `sodium.memzero`'d immediately (mirror of 1.7 evidence decrypt + 1.8 photo decrypt). No module-level cache; each export re-opens via `getActiveSigningKey()`. The plaintext-zero assertion is part of the §3.13 S4 test surface (spy-counts `sodium.memzero` calls against the two key opens — encryption + signing). Non-negotiable #4. | Bun GC may copy the buffer before `memzero` fires; same bound as 1.8 T-I23. |
| T-R21 | Signing-key rotation in 1.12 purges the prior `workplace_signing_keys` row instead of marking it `retired_at`, breaking verification of every past signed export | ADR-0008 §3.7: rotation INSERTs a new row with `active=true` and UPDATEs the prior to `active=false, retired_at=now()` — retired rows stay in the table forever. The migration linter rejects any `DELETE FROM workplace_signing_keys` diff. The manifest route reads by id regardless of `retired_at`. Runbook §"rotation" documents the procedure and the "retired keys are evidence" stance. Non-negotiable #2. | Operational; runbook + lint rule are the bounds. Rotation lands in 1.12 with the public verification page (§3.7). |
| T-R22 | The decrypted body buffer carrying supervisor names / accommodation context persists past the PDF response (mirror of 1.8 T-I23) | ADR-0008 §3.8: `pdf-shared` discipline applies — title + body + every response's author_role + body decrypt inside the render request lifetime using `sodium_malloc`-backed buffers, with `sodium.memzero` after `pdfkit.text()` returns. Workplace encryption private key is `memzero`'d at request end. The plaintext-zero assertion (§3.13 S4) covers both the long-form body buffer and the per-response buffers. Same posture as 1.8 T-I23 scaled to long-form prose. Non-negotiable #4 / #16. | Bun GC bound (same as T-R20 / T-I23); documented. JS string immutability means the buffer-derived string is the unavoidable cost — same documented tradeoff as 1.8. |
| T-R23 | A clause body fetched from the corpus contains characters that inject PDF markup or overflow the renderer's line-break logic when expanded into a footnote (the corpus is trusted but a future seed-fixture bug could land hostile UTF-8) | ADR-0008 §3.8 + 1.4 sec-F1 lineage: corpus fixtures are Zod-validated at seed time against `<` / `>` (priv-F2/sec-F1 carries forward). The `pdfkit.text()` API is text-only — no markup parser, no script surface, no `/JS` / `/JavaScript` / `/AA` / `/OpenAction` per 1.8 T-I25. Footnote text is treated as plain text via `pdfkit`'s default escaping. Golden-PDF test asserts a fixture with `<`/`>`/`{`/`}` renders as literal glyphs, not markup. | Library bug surface; mitigated by golden-PDF test on every PR that touches `pdf-shared/` or `recommendations/pdf-renderer.ts`. |
| T-R24 | The provenance footer's `chainIdx` placeholder is computed pre-replace and the post-replace pass mismatches it against the actual chain row index (mirror of 1.8 sec-F8 hash-replace pattern) | ADR-0008 §3.8 (per ADR-0007 §3.9 simplified pattern): footer carries `Chain idx {predictedIdx}` written in the first pass. The chain append uses `pg_advisory_xact_lock('audit_log.append')` and returns the actual `idx`; the route asserts `predictedIdx === actualIdx` inside the transaction and throws `ChainIdxMismatch` (transaction rollback, no Tigris PUT) on drift. Golden-PDF test asserts the rendered footer's idx matches the chain anchor's `audit_idx`. Non-negotiable #2 / #16. | Predicted-idx contention under concurrent appenders is bounded by the advisory lock (1.3 T-AC4 shape); single-co-chair scope makes contention negligible. |
| T-R25 | The ZIP bundle is non-deterministic — file timestamps in the ZIP central directory change per render, producing a different `output_sha256` for byte-identical signed content (defeats the chain anchor's integrity claim) | ADR-0008 §3.9: ZIP assembly uses a fixed epoch (`1980-01-01T00:00:00Z` — the ZIP-format minimum) for every entry's mtime, fixed `external_attr=0`, fixed entry order (`recommendation-<id>.pdf`, then `recommendation-<id>.pdf.sig`, then `manifest.json`). The bundle is hashed AFTER assembly and that hash is what the chain row + manifest carry. Byte-stability test (§3.13 S4) asserts two renders of identical inputs produce byte-identical ZIPs. | None material — deterministic-ZIP is a solved problem; the test guards against regression. |
| T-R26 | The Ed25519 signature is computed over the PDF bytes only; the manifest is unsigned, so an attacker can swap the manifest (changing `signing_key_id` to a compromised key whose private they hold) while leaving the legitimate signature + PDF intact, fooling a verifier who trusts the manifest's key id | ADR-0008 §3.9: the manifest is metadata about the signature, not subject to forgery in the sense that matters — a verifier MUST verify `sodium.crypto_sign_verify_detached(sig, pdfBytes, publicKey)` against the public key derived from `signing_key_fingerprint`, not from `signing_key_id` alone. The manifest endpoint cross-checks the fingerprint against the stored row; a swapped `signing_key_id` resolves to a different fingerprint and the verifier hits a fingerprint-mismatch. The chain anchor's `signing_key_id` is the durable record of which key was actually used. Runbook documents the verification recipe (fingerprint-first). | A verifier who trusts the manifest's key id without checking the fingerprint can be fooled — runbook is the bound. The PAdES upgrade (1.12) closes this structurally by embedding in PDF. |
| T-R27 | An attacker swaps `recommendation-<id>.pdf` inside the ZIP for a tampered PDF while leaving the .sig and manifest intact — the signature fails to verify (detection), but a careless recipient who only looks at the manifest might miss the failure | ADR-0008 §3.9: verification flow is fingerprint → fetch public key → `crypto_sign_verify_detached(sig, pdfBytes, publicKey)`; a tampered PDF flips the verify to `false`. The runbook's verification recipe (and the 1.12 public verification page) leads with "verification status" as the headline. The chain anchor's `outputSha256` (computed over the PDF bytes) is the secondary cross-check — a tampered PDF has a different hash than the chain recorded. **S5 sec-F3 close-out (T-R45):** the download path also extracts `signature.bin` from the ZIP, computes `sha256(signatureBytes)`, and cross-checks against `export_records.signature_sha256` (the chain-anchored signature hash). A consistent-ZIP-swap (attacker re-signs with a different key + rewrites manifest.pdfSha256 to match a tampered PDF) flips this to 500 `export_signature_tamper_detected`. The full Ed25519 crypto verify on every download remains a 1.12 hardening item; the SHA-256 cross-check is the cheap-and-correct close-out per the S5 reviewer. | Detection-only at the recipient; runbook + 1.12 public verifier page are the operational bounds. |
| T-R28 | The step-up freshness floor doesn't engage on the recommendation export route — a stale grant from `evidence.decrypt` or `inspection.export` (also 60s, different action) is silently accepted (mirror of 1.8 T-I30) | ADR-0008 §3.10: export routes call `checkStepUpFreshness({ maxAgeSeconds: 60, action: 'recommendation.export' })` for the create path and `'recommendation.export.download'` for the re-download path. The freshness check binds to the action string AND the elapsed time; a grant from a different action is rejected. `WWW-Authenticate` header carries `max_age="60"` (1.5 sec-F3 pattern). The export route opens TWO private keys (encryption + signing) — the 60s floor is the right posture for the privilege exercised. Non-negotiable #16. | None material — same shape as 1.8 T-I30. |
| T-R29 | A 60s step-up grant for `recommendation.export` of recommendation A is replayed within the window to export recommendation B, smuggling a different artifact under the same grant | 60s freshness floor enforced on every recommendation export route. The action string `recommendation.export.<id>` is reported in the WWW-Authenticate challenge header for the client's UX (the global step-up modal surfaces WHICH recommendation the grant is for), **but the server enforces only the (actor, freshness-window) tuple — NOT a per-action binding**. `checkStepUpFreshness` ignores the `action` parameter when matching grants. A fresh grant obtained for any prior step-up action within the 60s window IS accepted by any subsequent step-up gate. **S5 sec-F1 close-out (user-authorized doc-only fix):** the prior wording claimed per-recommendation binding that the implementation does not deliver; the honest stance is documented here and in `apps/api/src/auth/step-up.ts`'s inline contract comment. True per-action binding (storing `(action, until)` tuples on the session model + asserting cross-action rejection) is a 1.12 hardening item documented in `docs/runbooks/recommendations.md` §11. The integration test asserts the action-string content in the WWW-Authenticate header (the UX contract) — NOT cross-action rejection (which would be misleading given the current implementation). | Coarse-grained freshness instead of per-action binding; 1.12 backlog item closes the structural gap. |
| T-R30 | Re-download of an existing signed bundle (`GET /api/recommendations/exports/:id/download`) is invisible to the chain — re-downloads after the original `recommendation.exported` anchor leave no trail (mirror of 1.8 priv-F5 / `inspection.export.downloaded` residual that 1.9 was supposed to close) | **S5 sec-F2 close-out (T-R43):** the 1.9 ratchet adds both `inspection.export.downloaded` AND the symmetric `recommendation.export.downloaded` kind. The recommendation re-download handler emits a per-call anchor inside a db.transaction AFTER step-up clears AND Tigris fetch succeeds AND the TOCTOU verify (PDF hash + signature hash cross-checks per T-R45) passes AND BEFORE the bytes return. Failed paths (401, 404, 410, SHA mismatch, signature mismatch, wrong_kind) do NOT anchor. The chain row's `actor_id` slot pins the downloader's user id; the payload echoes `{exportId, recommendationId, downloadedByUserId}`. Cross-link: T-I31 inspection.export.downloaded for the structurally-identical sibling. Step-up gated at 60s + CSRF header per T-E14 / T-I30 pattern. | None material — the 1.9 ratchet closes the 1.8 residual on both surfaces. |
| T-R31 | The `export_records.kind` CHECK extension breaks existing 1.8 rows — the migration drops the old `CHECK (kind IN ('inspection_single','inspection_batch'))` and the new constraint rejects 1.8-era rows that fail the new column-nullness alignment | ADR-0008 §3.11: migration 0008 drops the old CHECK then re-adds `CHECK (kind IN ('inspection_single','inspection_batch','recommendation_single'))` + the column-nullness alignment CHECK in the same migration. The alignment CHECK is structured so existing 1.8 rows (with `inspection_ids IS NOT NULL` and `recommendation_id IS NULL`) satisfy it. Migration test loads a 1.8-era fixture, runs migration 0008, asserts the row still validates. The four new columns (`recommendation_id`, `signature_sha256`, `signing_key_id`, `citations_hash`) are nullable specifically to keep 1.8 rows valid. | None material — accepted tradeoff per §3.11 (4 nullable columns + 1 alignment CHECK is cheaper than a sibling table). |
| T-R32 | A `kind='recommendation_single'` row is INSERTed with a non-null `inspection_ids` array, misusing the 1.8 column for a recommendation export — confusion at the re-download path | ADR-0008 §3.11: the column-nullness alignment CHECK enforces `kind='recommendation_single' IMPLIES inspection_ids IS NULL AND recommendation_id IS NOT NULL AND signature_sha256 IS NOT NULL AND signing_key_id IS NOT NULL AND citations_hash IS NOT NULL`. A misuse fails at INSERT with 23514. Routes filter by `kind` before dispatching to the inspection vs recommendation re-download path. | None material — DB-layer backstop. |
| T-R33 | The re-download route serves a recommendation ZIP with `Content-Type: application/pdf` (or vice versa) — a browser inline-renders the ZIP as PDF, confusing recipients and breaking signature verification | ADR-0008 §3.8 + §3.11: download handler reads `kind` from `export_records`, sets `Content-Type: application/zip` + `Content-Disposition: attachment; filename="recommendation-<id>.zip"` for `recommendation_single`, and `application/pdf` + the inspections naming for `inspection_single`. Per 1.7 T-E15 close-out, `Content-Disposition: attachment` forces a download dialog (no inline render). Integration test asserts mime-type per kind. | None material. |
| T-R34 | The withdrawal handler emits `recommendation.withdrawn` with the rep's `withdrawalReason` text in the payload, leaking PI (supervisor names, accommodation context) into the immutable chain (mirror of 1.3 T-AC9 / 1.5 T-H7 typed-payload invariant) | ADR-0008 §3.1 + Compliance #4: the `AuditPayload` discriminated union in `packages/shared-types/src/index.ts` defines `recommendation.withdrawn` as `{recommendationId, linkedActionItemId?}` — the `reason` field is structurally not declarable; the typechecker rejects the field at every `append()` call site. The reason text lives only on `recommendations.withdrawal_reason_ct` (envelope-encrypted, step-up-gated read). The action_item's archive-move reason is the literal template `"Recommendation withdrawn."` (§3.5) — never the rep's free text. Same priv-F2 invariant as 1.3 / 1.5 T-H7 / 1.6 T-AI6 / 1.7 T-E7 / 1.8 T-I10. Non-negotiable #2 + #4. | None material — the union type is the structural enforcement. The 1.12 JSON-schema runtime reject layer is the deferred backstop. |
| T-R35 | The withdrawal flow uses shame-coded copy ("Are you sure you want to give up on this recommendation?") that discourages a rep from exercising their s.9(20) authority appropriately | ADR-0008 §3.1 + Compliance #7: the withdraw-confirm copy reads `"Withdraw recommendation. The linked action item will be archived. This action is recorded in the audit chain."` — no "are you sure," no "give up." Status-machine transitions are framed as record-keeping, not advocacy. The drafting form copy never discourages submission. Reviewed at S5 by the same rights-protective gate as 1.5 / 1.8 (per §3.13). Non-negotiable #7. | None material — copy is fixed in the route + UI; reviewer gate is the bound. |
| T-R36 | Tigris stores the signed ZIP bundle without `ServerSideEncryption` — the bundle (containing the signed PDF whose body decrypts to supervisor names and accommodation context) sits at rest in cleartext on Tigris (mirror of 1.8 priv-F1 close-out) | ADR-0008 §3.8: identical posture to 1.8 T-I40 close-out — `apps/api/src/evidence/tigris.ts` `buildPutObjectCommand` sets `ServerSideEncryption: 'AES256'` on the recommendation export PUT. The shared put-helper covers both kinds; no recommendation-specific bypass. Unit test asserts the SSE parameter is set on the recommendation export PUT. Non-negotiable #4 / #16. | Bucket-level default-encryption policy + Tigris lifecycle policy for `exports/*` deferred to 1.12 (mirror of 1.8 ops follow-up). |
| T-R37 | The route-level `expires_at = now() + 30 days` is the only TTL — without a Tigris bucket lifecycle policy, the cleartext-on-disk ZIPs survive past 30 days if the route-level cleanup misses them | ADR-0008 §3.8: `expires_at` is the primary TTL enforcer in 1.9 (same posture as 1.8 T-I40 close-out). The Tigris bucket lifecycle policy for `exports/<id>/*` is a 1.12 ops follow-up per the runbook. A 1.9 nightly sweep best-effort `DeleteObject`s any `export_records` row past `expires_at` (lands in S4). | Best-effort cleanup can miss; 1.12 bucket lifecycle is the structural close-out. Same residual shape as 1.8. |
| T-R38 | `evidence_files.linked_type` does not extend to `'recommendation'` in migration 0008 — the rep cannot attach evidence to a recommendation, leaving the polymorphic-FK ratchet's final value unconsumed (the 1.7 forward seam per `packages/shared-types/src/index.ts:260`) | ADR-0008 §3.13 + Consequences: migration 0008 opens `'recommendation'` at both the route allow-list AND the `evidence_files_linked_fk_guard` trigger in lockstep (same simultaneous-ratchet posture as 1.8 T-I14 for `'inspection_finding'`). Integration test asserts a `linkedType='recommendation' linkedId=<bogus uuid>` finalize call fails with 422 at the route, and a hand-crafted INSERT fails with 23514 at the trigger. Closes the fifth and final accepted `linkedType`. Non-negotiable #2. | None material — explicit ratchet; the runbook documents that future `linkedType` additions follow the same three-step recipe. |
| T-R39 | The retroactive `inspection_finding.read` audit kind leaves prior reads unanchored — 1.8 reads that decrypted findings before 1.9 deploy have no per-read chain rows, producing an apparent "gap" in the chain for findings accessed pre-1.9 | ADR-0008 §3.12: the `inspection_finding.read` kind is forward-only — pre-1.9 reads are bounded by the existing `inspection.created` + `inspection_finding.created` anchors (per 1.8 priv-F3 residual). The 1.9 runbook documents the discontinuity explicitly: post-1.9 finding reads carry per-read anchors; pre-1.9 reads are bounded by the 1.8 surface anchors and the step-up gate (which IS audited via `auth_events` → `audit_log` for the factor verification itself). No backfill of phantom anchors. Non-negotiable #2 — the chain is append-only. | Documented residual; the 1.8 T-AC2 phantom-row invariant (no inserted rows except via `append()`) is preserved. |
| T-R40 | The `responsible_party_user_id` migration on `inspection_findings` is data-destructive — adding the column drops the existing `responsible_party_ct` rows or breaks queries that expect the old shape | ADR-0008 §3.12: migration 0008 ADDs the `responsible_party_user_id uuid` column as nullable (no DEFAULT, no NOT NULL); the existing `responsible_party_ct` column is untouched. Pre-1.9 rows read as `responsible_party_user_id IS NULL + responsible_party_ct IS NOT NULL` per §3.12. New writes prefer `user_id` for internal owners; the encrypted-name path remains for external. CLAUDE.md "migrations are append-only" honored. Integration test loads a 1.8-era fixture, runs migration 0008, asserts every pre-1.9 row reads identically. | None material — additive migration, no UPDATE / DROP. |
| T-R41 | The `--check-inspections` flag on `audit-log-verify.ts` produces false positives — cross-referencing `inspection.exported` payloads against `export_records.output_sha256` flags legitimate rows because the comparison logic doesn't account for the 1.8 close-out's hash-replace pattern (T-I27) | ADR-0008 §3.12: the verifier reads `export_records.output_sha256` AS the canonical hash (the same value the chain anchor carries) — the 1.8 hash-replace is a one-pass operation; the stored `output_sha256` is what was anchored. The verifier does not re-render PDFs. Test fixture covers a known-good 1.8 export and asserts the verifier reports zero discrepancies. | None material — the verifier compares stored hash to stored hash. |
| T-R42 | A pathological response chain (>20 amendments) exceeds reasonable PDF render time, producing timeouts or memory pressure during signed export | ADR-0008 Risks: soft cap of 50 responses per recommendation, enforced at the route layer with 422 `response_cap_exceeded`. The cap is configurable per the runbook; the rationale (amendment-war pathological scenario) is documented. The renderer's response-appendix pass is bounded by the cap. | Cap is the bound; raising it is a deliberate config change. |
| T-R43 | Re-downloads of a recommendation export ZIP after the original `recommendation.exported` anchor leave no chain trail — a rep cannot prove "I downloaded it the day before I served it on the employer" (S5 sec-F2 close-out, mirror of T-R30) | The download handler emits a `recommendation.export.downloaded` chain anchor inside a db.transaction AFTER step-up clears AND Tigris fetch succeeds AND the TOCTOU verify (PDF hash + signature hash cross-checks per T-R45) passes AND BEFORE bytes return. Failed paths (401, 404, 410, SHA mismatch, signature mismatch, wrong_kind) do NOT anchor. PI-clean payload: `{exportId, recommendationId, downloadedByUserId}`. Mirror of the 1.8 `inspection.export.downloaded` retrofit. Integration test asserts the anchor fires on happy path AND does NOT fire on 401 / signature-mismatch paths. | None material — explicit ratchet. |
| T-R44 | Draft-state PATCH mutations on a recommendation leave no chain trail — a bad-faith rep can replace a citation while preserving the body wording, and the swap is invisible until export time (S5 sec-F4 close-out) | The PATCH handler computes `priorCitationsHash` from the existing `recommendation_citations` rows BEFORE any DELETE, then `newCitationsHash` from the new citation set AFTER INSERT (or the prior hash if citations are unchanged). Both hashes come from `computeCitationRowsHash` (a JOIN-free pure helper in `apps/api/src/recommendations/citations.ts` over the raw `(statute_code, clause_id, version_date, position)` triples). The `recommendation.draft_patched` chain row carries `{recommendationId, recommendationNumber, priorCitationsHash, newCitationsHash, bodyChanged}` — PI-clean. Emitted inside the PATCH transaction; failed PATCHes (404 / 422) do NOT anchor. `bodyChanged` is a boolean tracking whether the request included a `body` field (pragmatic — the route does not decrypt the existing body just to compare; the boolean is sufficient for the forensic surface the chain anchor records). Integration test asserts the anchor fires on citation-only change AND on body-only change. | None material — the union type in `packages/shared-types` is the structural backstop for the payload shape. |
| T-R45 | A Tigris-side attacker swaps the entire ZIP for a re-signed bundle (different signing key + recomputed manifest.pdfSha256 against the swapped PDF) — the manifest-PDF cross-check passes but the chain-anchored signing key id no longer matches what's in Tigris (S5 sec-F3 close-out) | The download handler extracts `signature.bin` from the ZIP alongside `manifest.json` and `recommendation.pdf`, computes `sha256(signatureBytes)`, and cross-checks against `export_records.signature_sha256` (the chain-anchored value). Mismatch returns 500 `export_signature_tamper_detected` with `reason: 'signature_sha_mismatch'`. The chain anchor's `signature_sha256` exists precisely to catch this — the SHA-256 cross-check is the cheap-and-correct close-out per the S5 reviewer. Full `crypto_sign_verify_detached` on every download is a 1.12 hardening item (runbook §11). The TOCTOU verify now spans THREE hashes (manifest's pdfSha256 == chain's outputSha256, observed PDF sha == chain's outputSha256, observed signature sha == chain's signature_sha256); any mismatch aborts before bytes return. Integration test simulates a tampered signature.bin in Tigris (mock) and asserts the 500. | Detection-only at the server; a 1.12 full crypto verify closes the structural gap. |

**Audit hooks for recommendations.** **Eleven** new event kinds added to `packages/shared-types` `AuditEventKind` (nine from ADR-0008 §3.13 S1 + two added in S5: `recommendation.draft_patched` and `recommendation.export.downloaded`). All payloads pass through the same typed-emit boundary as 1.3 priv-F2 — PI-clean by construction (IDs + hashes + counts + enums only, never names or free text):

- `recommendation.drafted` — `{recommendationId, jurisdiction}`. No title, no body. Emitted on draft create (the `recommendation.created` sub-state of `draft` per §3.1).
- `recommendation.draft_patched` (NEW S5) — `{recommendationId, recommendationNumber, priorCitationsHash, newCitationsHash, bodyChanged}`. PI-clean — two SHA-256 digests + a boolean. Emitted on every successful `PATCH /api/recommendations/:id` that mutated something (T-R44). The two hashes capture citation churn between PATCHes without exposing the citation text.
- `recommendation.submitted` — `{recommendationId, recommendationNumber, jurisdiction, citationCount, linkedActionItemId}`. No title, no body, no citation text. The `recommendationNumber` is the per-jurisdiction sequence (non-PI; identifies which Notice of Recommendation in the workplace's own series).
- `recommendation.response_captured` — `{recommendationId, responseId, position}`. No author role text, no body text. The `position` is the append index (§3.4).
- `recommendation.resolved` — `{recommendationId, linkedActionItemId, daysToResolve}`. Pairs with the action-items side's own `action_item.moved` anchor in the same transaction (§3.5).
- `recommendation.withdrawn` — `{recommendationId, linkedActionItemId?, fromStatus}`. No reason text (T-R34); reason is encrypted on the row. The `linkedActionItemId` is optional because withdrawal from `draft` has no linked action item (per §3.5).
- `recommendation.exported` — `{recommendationId, exportId, outputSha256, signatureSha256, byteSize, citationsHash, signingKeyId}`. The hashes are one-way integrity anchors; `citationsHash` is sha256 of the canonical-JSON of the resolved citations including each `body_hash` (§3.8) — this binds the exported PDF to the exact corpus state it was rendered against.
- `recommendation.export.downloaded` (NEW S5) — `{exportId, recommendationId, downloadedByUserId}`. PI-clean (T-R43 close-out, mirror of the 1.8 `inspection.export.downloaded` retrofit). Emitted on every successful re-download of a recommendation export ZIP after step-up + Tigris fetch + TOCTOU verify (PDF + signature) all pass.
- `inspection_finding.read` — `{findingId, inspectionId, readerUserId}`. PI-clean (§3.12 absorbs the 1.8 priv-F3 residual). Emitted on every successful `GET /api/inspections/findings/:id` after step-up.
- `inspection.export.downloaded` — `{exportId, downloaderUserId}`. PI-clean (§3.12 absorbs the 1.8 priv-F5 residual). Emitted on every re-download of an inspection export ZIP/PDF. The recommendation re-download path emits a structurally identical anchor scoped to the recommendation export (T-R30).
- `audit.workplace_signing_key.seeded` — `{signingKeyId, fingerprint, createdAt}`. Emitted by the first-run seed of the initial Ed25519 signing keypair (§3.7); a future 1.12 rotation will pair an `audit.workplace_signing_key.rotated` kind on the same template. The `fingerprint` is `SHA-256(public_key)` — a public, non-PI integrity anchor.

**Corpus-amendment invalidation is handled structurally, not by the recommendation surface.** A recommendation's citation rows pin to the exact `(statute_code, clause_id, version_date)` triple at submit time (T-R8); the corpus's append-only-version invariant (1.4 T-LC3 + T-LC5) guarantees that historical triple remains queryable forever; the signed PDF's footnote provenance and the chain anchor's `citationsHash` both encode that pinned state. A re-seed that publishes an amendment produces a different `citationsHash` on a fresh export of the same recommendation, and the chain records both — the original signed PDF remains verifiable against the historical clause row, and the verifier recipe (runbook) leads with "resolve `(statute_code, citation, version_date)` against the corpus, not the live active version."

### 2.10 Offline-first sync threats (Milestone 1.10)

Offline sync is the first surface that moves data through **three trust zones in one logical write**: the rep's untrusted-at-rest IndexedDB on a phone that can be lost, stolen, or seized; the `sync_queue` envelope that crosses the network when the bar returns; and the server-side `sync_idempotency` ledger + chain anchor that lands at queue-drain time, NOT at type-time. The threat surface is the union of 1.5–1.9 (every prior route is now offline-queueable and takes a `clientId` + `Idempotency-Key` + `If-Match` ratchet), plus four new surfaces: the local Dexie store that holds envelope-encrypted ciphertext + plaintext metadata for every entity, the queue retry curve + 48h dead-letter ceiling, the three-way merge conflict UI that requires step-up for encrypted fields, and the service worker that intercepts `/api/*` mutations to return synthetic 202 / 503 responses while offline. The most consequential semantic shift is on the chain anchor itself: the chain proves "server received this operation at time T," not "rep typed this at time T'" — the 21-day s.9(21) clock measures from server `submitted_at` (§3.12), and the runbook surfaces the arbitration framing. Every mitigation below cites the ADR-0009 subsection or the CLAUDE.md non-negotiable it implements.

| # | Threat | Mitigation | Residual |
|---|---|---|---|
| T-S1 | A rep's phone is lost, stolen, or seized — forensic dump of IndexedDB recovers every sensitive field across hazards, action items, inspection findings, recommendations, and evidence files (CLAUDE.md #4) | ADR-0009 §3.1: every sensitive field is envelope-encrypted at write time under the workplace public key (same sealed-box envelope as 1.7 evidence). The workplace private key never leaves Fly Secrets; a stolen phone yields only ciphertext for `title_ct`, `body_ct`, `description_ct`, `observation_ct`, `corrective_action_ct`, `reporter_identity_ct`, `_local_ciphertext_b64` on evidence rows, etc. Postgres-side and Dexie-side ciphertext are the same wire format. | A forensic adversary with the workplace private key (Fly Secrets compromise) can decrypt everything that has ever been in the cache — same bound as 1.5 T-H1 / 1.7 T-E1. KEK rotation procedure is the operational mitigation. |
| T-S2 | The same forensic dump recovers plaintext non-PI metadata — GPS coordinates on `evidence_files` local rows, entity ids, timestamps, status enums, severity, citation markers `[[cite:N]]`, recommendation numbers, `has_observation` / `has_corrective_action` booleans, queue-typed-at times, attempt counts — and the pattern-of-life leak (when the rep typed, where they were, which entity touched which) is a tracking surface independent of the encrypted bodies | ADR-0009 §3.1 + §"Negative tradeoffs": the metadata leakage is the documented tradeoff. The local projection shape mirrors the server's projection — no new plaintext field is introduced on the client that wasn't already projected on the server. GPS is capped at NUMERIC(8,4) (~11m, 1.7 T-E5) before it lands in the cache, so the cached coordinate set has the same resolution bound as Tigris. 1.12 absorbs the structural fix (WebCrypto + IndexedDB-encryption wrapper); 1.10 ships with the metadata residual called out in the runbook. | Pattern-of-life visible to a forensic adversary; deferred to 1.12 per the §"Don't" guardrail. Documented. |
| T-S3 | A future PR adds a convenience helper that caches decrypted plaintext in Dexie ("just for the active session, for performance"), breaking the never-persist-decrypted contract and turning the stolen-phone bound from ciphertext to plaintext | ADR-0009 §3.1: decrypted plaintext lives in JS heap memory only for the lifetime of a reveal view; it is NOT persisted to Dexie. The reveal route is always live-fetched from the server. The Dexie store's TypeScript shape exports no `*_pt` column on any entity table — the typechecker rejects a PR that tries to add one. The 1.7 sec-F9 close-out's `sodium.memzero` pattern carries over: the encrypt-write closes out the plaintext buffer on the queue-write path too (§3.2). | Type discipline is the bound; runtime check is deferred to 1.12 (same posture as 1.3 priv-F2 typed-emit boundary). |
| T-S4 | The queue retries on 5xx / network failure; without idempotency a retry that drained but failed to return the response (network blip on the response leg) double-writes — two hazards, two `position=2` response rows, two `action_item.moved` chain anchors for what is evidentially one move (CLAUDE.md #2) | ADR-0009 §3.4: `Idempotency-Key` middleware wired BEFORE auth, body, and rate-limit in `apps/api/src/index.ts` so a replay short-circuits BEFORE consuming auth or rate budget. The key is the queue's `payload_hash = sha256(canonical_json(payload))`. Cache hit returns the prior `response_status_code` + `response_body` verbatim WITHOUT re-running the handler — the chain anchor was emitted on the first run and is NOT re-emitted. The 1.7 `evidence.uploaded` + 1.6 `action_item.moved` + 1.9 `recommendation.submitted` chain anchors all preserve their once-per-logical-operation invariant. | Cross-machine drift: if the chain landed, the response was lost, AND the idempotency row was lost (e.g., DB restore from before the cache write), a replay re-emits. Bounded by the 48h dead-letter + the 7-day TTL window (T-S6); documented residual. |
| T-S5 | The middleware order is wrong on a future deploy (auth runs first) — a retry that lands on the idempotency cache still consumes the rep's auth-attempt budget + the per-IP rate-limit tokens; a malicious actor probes the cache by spamming keys, draining the rate limit for legitimate retries (ADR-0009 §3.4) | ADR-0009 §3.4 specifies the exact order in `apps/api/src/index.ts`: `csrfHeaderGuard()` → `idempotencyKeyGuard()` → `authMiddleware()` → `rateLimit()` → route handlers. The idempotency middleware short-circuits BEFORE auth on a cache hit. The cache lookup is keyed on `(key, user_id)` — an unauthenticated probe cannot hit a cached row because there's no `user_id`. Integration test asserts a replay returns the cached response without bumping `login_attempts` or the rate-limit token bucket. | A misconfigured middleware order would re-introduce the budget consumption; CI integration test is the bound. |
| T-S6 | `sync_idempotency` TTL is 7 days; a queue operation that survives a 48h dead-letter + manual rep intervention + a long network outage hits the cleared idempotency row 8+ days later, re-runs the handler, double-emits the chain anchor for an operation that already landed (ADR-0009 §3.4) | ADR-0009 §3.4: the 48h dead-letter ceiling (T-S10) is well inside the 7-day TTL window, so no operation drains via the queue worker past the TTL. The pathological case is a rep who manually retries a dead-letter row after >7 days — runbook covers this: the dead-letter UI surfaces the original `payload_hash`; a manual retry re-uses the same key; if the server has cleared the row, the handler re-runs. Single-tenant at single-co-chair scope makes this near-zero. The TTL sweep job is 1.12 (the table grows monotonically through 1.10); until then the practical TTL is effectively unbounded. | Documented residual; runbook §"Dead-letter recovery" covers it. |
| T-S7 | Two reps' devices (or one rep's two devices) edit the same hazard / action_item / inspection / recommendation; one drains first, the second's PATCH overwrites silently — the chain anchor records the second write as canonical, the first rep's edit vanishes with no conflict surface (CLAUDE.md #2) | ADR-0009 §3.7: every updatable entity table gains a server-side `version` integer (auto-incremented via trigger on UPDATE). PATCHes carry `If-Match: <etag>` where the etag is base64(`version`); server `SELECT version FOR UPDATE`, returns 409 `{error: 'version_conflict', server_version, server_state, base_version}` on mismatch. The client's queue worker writes a `sync_conflicts` row + flips `_sync_state='conflicting'`; the amber sync-status chip surfaces the conflict for manual resolution. A PATCH without `If-Match` returns 428 `precondition_required`. | The web client + queue worker always set `If-Match`; a hand-crafted unauthenticated client cannot bypass (auth gates the route). Integration test asserts a stale-etag PATCH returns 409. |
| T-S8 | The three-way merge UI loses the base state — Dexie eviction under quota pressure (or a fresh-install on a new device that doesn't have `_base_state`) degrades the merge to two-way; the rep can't see what they changed from, so "keep mine" silently overwrites a real edit the rep doesn't remember making (ADR-0009 §3.7) | ADR-0009 §3.7: `_base_state` is the snapshot of the row at last `clean`, written on every successful sync. On eviction (or on a device that never had the base), the conflict UI degrades gracefully to a two-column "yours vs theirs" diff with explicit copy: _"The base version is no longer available on this device. Compare your local state against the server's current state."_ The rep makes an informed choice; no silent merge. The chain anchors both pre- and post-resolution states (the PATCH that lands carries the merged result + a fresh `If-Match` matching the server's current version). | Two-way degradation is a UX bound, not a security bound; documented in the runbook. |
| T-S9 | The single-thread queue worker drains out of order — operation A (create finding) and operation B (promote that finding to action_item) race; B drains before A and the server FK fails because the finding doesn't exist yet (ADR-0009 §3.2) | ADR-0009 §3.2: the queue worker runs in the main thread (Dexie does not transfer across the structured-clone boundary; no Web Worker). Hot loop pulls batches of 10 rows ordered by `next_attempt_at` ASC then by `id` ASC (creation order); operations enqueued in causal order drain in causal order. A foreign-key failure at drain (race against a still-pending parent) marks the child as 5xx-retry, the parent drains on the next pass, the child retries successfully. The advisory-lock + transaction discipline from 1.6 T-AI5 + 1.9 T-R10 (per-section sequence allocation) is unchanged — the queue layer is plumbing, not provenance. | A bug in the queue worker that re-orders mid-batch would break causal order; covered by integration test (S4) that drains a finding-then-promote pair under simulated network flap. |
| T-S10 | A malicious server (or a misconfigured one) returns 503 to every drain attempt; the queue exhausts the backoff curve and rows land in the 48h dead-letter; the rep loses confidence that anything is syncing (ADR-0009 §3.2) | ADR-0009 §3.2: backoff curve is `[1s, 5s, 30s, 5min, 30min, 2h, 12h, 24h]` (pure function in `apps/web/src/sync/backoff.ts`); after 8 attempts (~48h) the row is `dead_letter = true`. The dead-letter UI in `apps/web/src/sync/sync-status-view.tsx` surfaces every row with the server's error code + a "retry" / "delete local" action. The amber paused chip is the rep's signal. The rep is the authority on recovery — the app does not auto-decide. | A rep who ignores the amber chip for >48h loses chain anchors on critical events (signed recommendations, signed inspections); the runbook covers operator-side dead-letter recovery for common error codes. |
| T-S11 | A `SyncOperation` is captured + replayed (e.g., via dev tools network log or a malicious browser extension); the server re-processes the operation and emits a duplicate chain anchor (CLAUDE.md #2) | ADR-0009 §3.4: every mutation carries `Idempotency-Key: <payload_hash>`. A replay hits the `sync_idempotency` cache and returns the cached response without re-emitting the chain anchor. The `(key, user_id)` cache key prevents a malicious replay from a different actor (a different rep with the same captured key is a different cache miss; the route still runs but the auth check rejects an unauthenticated replay; an authenticated replay from a different actor produces a different chain row with that actor's user_id — same shape as any legitimate write from that actor). | Same-actor replay is structurally one chain row; cross-actor replay produces a chain row attributed to the replaying actor — the chain captures the truth. Documented. |
| T-S12 | A buggy client RNG (or a deliberately-tampered dev-tools fingerprint) generates predictable `_local_id` UUID v4s that collide with existing rows; the server's 409 `id_collision` path is hit, but the rep's UI doesn't surface a clean recovery (ADR-0009 §3.3) | ADR-0009 §3.3: the server validates `clientId: z.string().uuid()` (rejects non-RFC-4122 strings); rejects duplicates with 409 `id_collision`. The client-side queue worker marks 409 `id_collision` as `dead_letter = true` (auto-retry would produce the same rejection) and the dead-letter UI prompts the rep to "regenerate id and retry" — a one-tap action that allocates a fresh `_local_id`, copies the local row's content to the new id, re-enqueues, and deletes the old conflicting row. 122 bits of entropy in a healthy RNG makes accidental collision ~0; a deliberately-tampered RNG is detected by the server's UNIQUE constraint. | A rep with a fully-compromised browser has many worse problems; documented. |
| T-S13 | A client supplies a `clientId` that doesn't match any `_local_id` in the local store — a hand-crafted forgery — and the server INSERTs the row with the forged id; the chain anchor binds whatever the client sent (ADR-0009 §3.3) | ADR-0009 §3.3: the server cannot distinguish a forged `clientId` from a legitimate device-generated one (both are UUID v4 strings). The mitigation is structural: the actor's `user_id` is bound by the auth cookie + the JWT, NOT by the client; the chain anchor records `actor_id = auth.userId` regardless of what `clientId` was supplied. A forged `clientId` therefore attributes a row to the actor who sent the request — same shape as any legitimate write. The multi-rep concern (a rep aliases another rep's id) is deferred to the multi-rep forward seam (1.5 T-H10 / 1.6 T-AI9 / 1.9 T-R21); single-co-chair scope makes this non-actionable in 1.10. | Multi-rep forward seam; documented. |
| T-S14 | A foreign-key write order race: operation A enqueues a finding with `inspection_id = <_local_id>`; operation B (create inspection) is enqueued AFTER A; the queue drains A first, the server FK fails (ADR-0009 §3.2 + §3.3) | ADR-0009 §3.2: the queue worker is single-thread and drains in enqueue order (`id ASC` within the same `next_attempt_at` window). Within a session a child create cannot be enqueued before its parent because the optimistic-UI flow requires the parent's `_local_id` to exist in Dexie before the child references it. A 5xx FK failure on a race (e.g., a rare ordering inversion from a manual cache poke) marks the child for retry; the parent drains, the child retries, the FK resolves. The 1.7 → 1.8 polymorphic FK ratchet posture (T-I14 / sec-F4) is unchanged — the route layer + the trigger validate at drain time. | Documented; integration test covers the inspection→finding chain. |
| T-S15 | The cached idempotency response carries `Set-Cookie` (a step-up grant, a session refresh) that no longer applies — a replay returns the cached headers and re-applies a stale grant (ADR-0009 §3.4) | ADR-0009 §3.4: the cached `response_body` is the JSON body only; `response_status_code` is the status; `Set-Cookie` headers are NOT cached. The idempotency-replay path returns the cached body + status but freshly minted (or absent) cookies from the current request context. Step-up grants live in the session JWT's claims (not in the response body) — a replay never re-grants step-up. Auth refresh has its own dedicated route which is NOT queueable (per §3.6 "require-online" list), so no auth-lifecycle response is ever cached. | None material — the cache is body+status only. |
| T-S16 | Payload-hash collision: the queue's `payload_hash` doesn't include `action_kind` + `entity_local_id` + `actor_id`, so two structurally-different operations could collide on body alone (ADR-0009 §3.4) | ADR-0009 §3.4: `payload_hash = sha256(canonical_json(payload))` where the canonical payload includes the action kind, the route, the body fields, and (server-side) the `(key, user_id)` cache key. The middleware cross-checks `payload_hash` on cache hit — if the rep replays the same key with a different body (extremely unlikely; the queue derives the key from the body), the middleware returns 409 `idempotency_key_payload_mismatch` rather than the cached response. Defense against a future bug in key derivation. | SHA-256 collision probability is negligible; the cross-check is the defense against a derivation bug. |
| T-S17 | An endpoint claimed "queueable" actually needs a server-side capability that can't run offline — e.g., the action_item per-section sequence allocation needs `pg_advisory_xact_lock` at drain time, but the queue serializes the lock acquisition into a single drained insert (ADR-0009 §3.6) | ADR-0009 §3.2 + §3.6: the queue worker is single-thread; the lock allocation happens server-side at drain time inside the route's transaction (same shape as 1.6 T-AI5 sequence allocation). The optimistic UI shows "Action item #pending" until the drain completes; the canonical sequence number lands when the chain anchor fires. The `recommendation_number` allocation (1.9 T-R10) uses the same pattern. Integration test asserts a queued create with a pending sequence drains correctly and the chain anchor carries the allocated number. | The optimistic-UI "pending" state is the rep-visible bound; documented in §3.12 copy. |
| T-S18 | A `reveal` endpoint is queued accidentally (a UI bug calls the typed client's queue path for a GET-decrypt) — the queue stores the reveal request, replays it on network return, and the rep is shown stale-at-best plaintext (ADR-0009 §3.6) | ADR-0009 §3.6 + §3.9: the service worker classifies routes; every `reveal` / decrypt route is in the "require-online" list. An offline call returns a synthetic 503 `network_required` body — the SW does NOT enqueue. The typed client's read-path also bypasses the queue (reads are not mutations; the SW only queues mutations). A UI bug that calls the queue path for a reveal hits a route Zod schema that doesn't accept queued reveals — the queue worker would dead-letter on 400. Integration test asserts a `GET /api/recommendations/:id/reveal` while offline returns synthetic 503 + the UI shows "Network required." | UI bound; the SW's classification is the defense. |
| T-S19 | A PDF export is attempted offline — the rep taps "Export" expecting it to queue, the SW returns synthetic 503, the rep tries again offline and sees the same error (ADR-0009 §3.6 + CLAUDE.md #16) | ADR-0009 §3.6: PDF exports are require-online (step-up + workplace private key + server-side render + Tigris SSE PUT + signed-bundle assembly — none of these have offline analogues). The SW returns synthetic 503 `network_required` and the UI shows a "Network required to export" banner per §3.11. The rep cannot accidentally bypass step-up + the chain anchor by queueing an export. CLAUDE.md #16 (step-up + audit on every export) is preserved structurally. | UX bound; the runbook covers the rep-facing framing. |
| T-S20 | A recommendation `submit` is queued offline; the rep believes the 21-day s.9(21) clock starts when they typed at 10:14am; the queue drains at 02:47am the next day and the chain anchor records `submitted_at = 02:47am`; the rep miscounts the deadline by the queue-delay window and misses the response window (CLAUDE.md #2 + ADR-0009 §3.12) | ADR-0009 §3.12: the optimistic submit view shows _"Recommendation submitted (pending sync). 21-day clock starts on sync."_ When the queue drains, the view updates to _"Recommendation submitted 02:47am on 2026-06-03. 21 days remaining. Response due by 2026-06-24."_ The chain anchor's `submitted_at` is the canonical legal timestamp; `computeRecommendationDeadline` (1.9 T-R10) is a pure function in `packages/shared-types` that reads from server-stamped `submitted_at`. The runbook documents the OHSA s.9(21) legal reasoning — the clock starts when the rep delivers to management, which is structurally after the queue drains. | Rep discipline is the bound on miscounting before the drain copy updates; documented. |
| T-S21 | An inspection signature is captured offline at 10am; the queue drains at 8pm; the chain anchor records 8pm as the signature time; in arbitration the signature's "when" diverges from the rep's reality (CLAUDE.md #2 + ADR-0009 §3.6) | ADR-0009 §3.2 + §3.6: the chain anchor binds the SERVER's `now()`, which is when the operation actually landed in the canonical record. The local Dexie row carries `_local_created_at` (the rep's device clock) as a UI-only hint, NOT as evidence. The runbook documents the arbitration framing: the chain proves server-receipt time; the rep can produce queue-typed-at via their device-side `created_at` column on the queue row + corroborating evidence (location, witnesses), but the chain is the canonical evidentiary surface. Non-negotiable #2 — every chain row's `ts_ms` is server-side and tamper-evident. | Two timestamps in evidence (queue-typed-at + chain `ts_ms`); arbitration adjudicators are guided by the runbook. |
| T-S22 | The reveal endpoint plaintext is cached in Dexie by a misconfigured background refresh — a list-view refresh fetches `/api/recommendations` and the response carries some decrypted preview field that lands in Dexie (ADR-0009 §3.1 + §3.5) | ADR-0009 §3.1: list projections from the server are non-PI metadata only (status enums, ids, timestamps, citation markers, `has_observation` booleans). No reveal-decrypt response is ever cached by the SW (§3.9 — `decrypt` URLs are explicitly excluded from cache). The background-refresh path (§3.5) writes to Dexie only the same shape as the server's list projection — no plaintext bodies. TypeScript shape rejects a list-row with a `*_pt` field at the typechecker boundary. | Type discipline + SW exclusion list are the defenses; same as T-S3. |
| T-S23 | A list view reads from Dexie and renders a stale row; the rep makes a decision (promote a finding, withdraw a recommendation) based on stale data; the chain anchor records the decision but the rep's reality was a different state (ADR-0009 §3.5) | ADR-0009 §3.5: every read fires a background refresh; the conflict-resolution model (T-S7) catches a divergent state at write time via `If-Match`. The PATCH carrying the rep's decision includes the etag the rep saw on Dexie; if the server has moved forward, the 409 surfaces and the conflict UI lets the rep re-decide against fresh state. The chain anchor never records a write that was based on a now-impossible state because the version check is server-side. | Stale reads inform a rep's choice but cannot land an inconsistent write; documented. |
| T-S24 | Local cache poisoning via XSS — a malicious script in the page writes garbage to Dexie; the next read returns garbage; an authenticated rep with their own factors writes the garbage back to the server, attributing it to themselves in the chain (CLAUDE.md #2) | ADR-0009 §3.1 + §3.9: the strict CSP from 1.2 §"Headers & Transport" + the 1.4 sec-F1 close-out (no `dangerouslySetInnerHTML`, strict XSS pre-filter on all user-supplied text in the legal corpus + 1.5–1.9 fields) bound the XSS surface. The SW's same-origin scope prevents a cross-origin script from registering itself. A successful XSS that writes garbage to Dexie is an XSS-class problem with the same posture as any session-bound exfil — the rep's session is the bound, not the cache. The chain anchor records the actor's intent (the actor whose session signed the request) regardless of cache poisoning. | XSS-class residual; same bound as the rest of the app. |
| T-S25 | A `_sync_state = 'clean'` row diverges from the server silently — the background refresh failed (network error, server 500, SW cache stale) and the rep reads from the local-stale row indefinitely (ADR-0009 §3.5) | ADR-0009 §3.5: background refresh fires on view mount, window focus, every successful drain, pull-to-refresh, and manual sync. A refresh failure logs to the sync-status chip's "last refresh failed" hint; the chip stays green only when the last refresh succeeded. The 30-second poll while online + non-empty queue (§3.2) catches a network blip; a stuck refresh moves the chip to "Offline" within one poll window. | Documented; the sync-status chip is the rep-facing signal. |
| T-S26 | Workplace public key has rotated between offline-capture and finalize; the rep's offline-typed body is encrypted under the OLD key; the server's finalize rejects at the 1.7 sec-F5 active-key check (ADR-0009 §3.8 + 1.7 sec-F5) | ADR-0009 §3.8: the workplace public key is cached in the SW (§3.9); cache refresh on every successful sync. A rotation produces a new `workplaceKeyId`; the rep's first online connection refreshes the cache; further offline-capture happens under the new key. The pathological window is "rotation happens during an offline session that started before rotation" — the queue's finalize call hits 422 `workplace_key_retired`, the queue marks `dead_letter = true`, the dead-letter UI prompts the rep to "re-capture under fresh key" (the original ciphertext on the device is re-encrypted under the new public key + re-enqueued). The 1.12 rotation script (1.7 T-E8) seals the structural gap. | A rep offline through a rotation must re-capture; documented in the runbook's KEK rotation procedure. |
| T-S27 | The three-step evidence upload (presign → PUT → finalize) leaves orphan ciphertext in Tigris when finalize fails (mirror of 1.7 T-E11 / sec-F11 orphan ciphertext, made worse by per-step queue retries) (ADR-0009 §3.8) | ADR-0009 §3.8: the three steps are one `sync_queue` row with an internal `step` counter; a failure at step (c) finalize retries finalize (not re-PUTs the ciphertext). Tigris-side orphans (PUT succeeded, finalize never landed) inherit the same residual as 1.7 — the 1.12 nightly orphan-sweep script reaps them via `evidence_files` left-join `Tigris ListObjects`. Until 1.12, an orphan is bounded by the bucket-side `expires_at` lifecycle policy (1.8 sec-F6 close-out + 1.7 sec-F11 inline comment). The chain anchor never fires for an orphaned upload — `evidence.uploaded` is emitted inside the finalize transaction after the row insert. | Documented; runbook §8 covers the cleanup follow-up. |
| T-S28 | Plaintext capture buffer lifetime in the offline encrypt+queue path is longer than the 1.7 sec-F9 close-out promises — the encrypt-then-queue is slower than encrypt-then-network and the plaintext bytes live longer in JS heap (ADR-0009 §3.8 + 1.7 sec-F9) | ADR-0009 §3.8: the encrypt call is the only consumer of plaintext bytes; immediately after `crypto_box_seal` returns, the plaintext buffer is `sodium.memzero`'d (same shape as 1.7 sec-F9). The queue write takes the ciphertext only — `sync_queue.payload_ct` carries the sealed bytes; `_local_ciphertext_b64` on the evidence row is the same sealed bytes. The queue-write latency does not extend plaintext lifetime; the zero happens before the queue insert. Integration test (S4) spy-counts `memzero` calls against the capture count. | Same bound as 1.7 sec-F9; Bun GC copy-before-zero is the residual. |
| T-S29 | The `_local_ciphertext_b64` field carries the same sealed-box envelope as Tigris-side ciphertext; a forensic dump that recovers it has the same posture as a Tigris compromise — but the local field also reveals which evidence has been captured but not yet uploaded, leaking timing of capture (ADR-0009 §3.8) | ADR-0009 §3.8: `_local_ciphertext_b64` is deleted from the Dexie row immediately after the finalize step lands successfully (the ciphertext is now on Tigris; keeping a local copy is unnecessary disk pressure). Pre-drain captures retain the local ciphertext; this is structurally the same posture as Tigris-side ciphertext (1.7 T-E1). Capture timing is part of the metadata leak (T-S2) — accepted residual. | Same bound as T-S1 / T-S2; documented. |
| T-S30 | A compromised CI pipeline ships a poisoned service worker — the SW intercepts every `/api/*` mutation and exfiltrates the (encrypted) queue payloads or rewrites them under an attacker-controlled key (ADR-0009 §3.9 + CLAUDE.md #3) | ADR-0009 §3.9: the SW is generated by `vite-plugin-pwa` at build time; the generated `sw.js` is build-hash-addressed; the source for the SW is in-repo. CI is the supply-chain bound — the same posture as any front-end build. The queue payloads are sealed under the workplace public key; even a poisoned SW that exfiltrates the queue ciphertext gets only sealed bytes (workplace private key on the server). A poisoned SW that rewrites the queue under an attacker-controlled public key would produce ciphertext the server cannot decrypt — finalize 422s, the chain anchor doesn't fire. CI integrity is governed by the existing pre-merge controls (gitleaks, pnpm audit, lockfile review, no analytics SDKs). | Supply-chain bound — same as the rest of the app. |
| T-S31 | The SW serves a stale app shell indefinitely while the rep stays offline — a critical security fix shipped yesterday is not on the device; the rep operates on a known-vulnerable bundle (ADR-0009 §3.9) | ADR-0009 §3.9: `vite-plugin-pwa` defaults include `skipWaiting + clientsClaim` and a `controllerchange` UI toast on bundle change. While the rep stays offline they cannot fetch the new bundle — the stale shell is the bound. Online return triggers the SW update; the toast surfaces "Update available" within seconds. The runbook covers force-refresh and the bundle-version check (the `GET /api/health` response carries the expected client version; a mismatch dumps the rep to a forced reload). 1.12 absorbs the SW rotation procedure for KEK rotation events. | Documented residual; online return closes the window. |
| T-S32 | The legal corpus cached in the SW is stale (the corpus was amended overnight; the rep drafts a recommendation offline citing a clause that's been retired) (ADR-0009 §3.9 + 1.4 T-LC3) | ADR-0009 §3.9: legal corpus has a 24h TTL OR per-session refresh whichever sooner; the SW catches new versions on background-refresh. The 1.9 T-R8 corpus-amendment-invalidation behavior is the server-side gate — at submit time the route re-validates the citation triple against the live corpus and rejects retired versions with `citation_not_in_corpus`. A rep who drafts offline against a retired clause hits this 422 at drain time; the dead-letter UI surfaces the rejection + the rep updates the citation. The 1.4 T-LC5 append-only-version invariant guarantees historical triples remain queryable for legitimate pinned citations. | Wasted drafting time is the bound, not a chain-of-custody issue; documented. |
| T-S33 | Tigris cross-origin PUTs are not intercepted by the SW; a malicious browser extension intercepts the cross-origin PUT and exfiltrates ciphertext or substitutes a different blob (ADR-0009 §3.8 step b + §3.9) | ADR-0009 §3.8 + §3.9: SW scope is the app origin only; Tigris PUTs are foreground `fetch` from the queue worker. The presign URL is 5-min TTL (1.7 T-E9); the finalize step verifies `ciphertext_sha256` against what the legitimate browser computed — a substituted blob fails the SHA-256 cross-check and finalize 422s. A browser-extension-class attack has the same posture as 1.7 T-E9 + T-E14 (the `X-Requested-With` header gate). | Same bound as 1.7 evidence upload; the SHA verification is the integrity anchor. |
| T-S34 | The synthetic 202 response for offline mutations confuses the typed API client — a route handler downstream of the client expects 200/201 only and the offline-write path silently fails (ADR-0009 §3.9 follow-on) | ADR-0009 §3.9: the typed client is wrapped to treat 202 as "queued, not committed" — the response body carries the optimistic local row + a `queued: true` flag. Integration test asserts every prior 1.5–1.9 route's client wrapper handles 202; the type system rejects a consumer that destructures the response as if it were 200/201. The SW's synthetic 202 body matches the schema the route returns on a real 201 (id + the optimistic state) so the UI renders the optimistic row identically. | None material — type system + integration test are the defense. |
| T-S35 | The synthetic 503 body returned by the SW for require-online endpoints leaks payload that mimics a real server response (the UI confuses real 503 from a server outage with synthetic 503 from offline) (ADR-0009 §3.9 follow-on + §3.11) | ADR-0009 §3.9: synthetic 503 carries `{error: 'network_required', source: 'service-worker'}` — the `source` field disambiguates from a real server 503. The UI's "Network required" banner reads the `source` field; a real 503 surfaces a different "Server unavailable" banner. The chain anchor never sees a synthetic 503 (the request never left the device). | None material — explicit body disambiguation. |
| T-S36 | The SW's stored request replay (the queued operation) omits the `X-Requested-With: jhsc-web` CSRF header from 1.7 sec-F2 / T-E14, opening the queued operation to passive cross-origin element loaders at replay time (ADR-0009 §3.9 follow-on + 1.7 sec-F2) | ADR-0009 §3.9: the SW stores the full request including all headers when queueing; the replay reconstructs the request with the original headers + injects the `Idempotency-Key`. The `X-Requested-With` header is preserved. Integration test asserts a queued POST replayed at drain time carries `X-Requested-With: jhsc-web`. The SW does NOT serve `<img src>` / `<iframe src>` from the queue — only same-origin `fetch` from the typed client paths enqueue. | None material — header preservation is part of the queue contract. |
| T-S37 | PWA install prompt phishing — a malicious overlay on a same-origin page mimics the native install prompt to capture credentials (ADR-0009 §3.10) | ADR-0009 §3.10: Chrome's native `beforeinstallprompt` dialog is browser-chrome (unspoofable from page content); iOS has no programmatic install at all — the rep follows the share-sheet → Add to Home Screen flow which is OS-chrome. A page-level overlay cannot replicate browser chrome. The app's "Install" button is text + a Lucide icon (no marketing flourish per CLAUDE.md #10); the click triggers the native prompt (Android) or a documentary modal (iOS). Documented as low-risk. | Browser-chrome bound; same as any PWA. |
| T-S38 | PWA-installed-mode permission scope diverges from browser-tab — camera permission persists longer in PWA mode and a rep grants once, subsequent unprompted captures fire (ADR-0009 §3.10) | ADR-0009 §3.10: this is OS-level permission semantics, not actionable in our code. The capture-to-record button shows a permission-state indicator (Lucide camera icon + a small status dot) so the rep can see whether camera is granted. The 1.7 evidence capture flow's "capture happens in-page, not via OS picker" (1.7 T-E4) is unchanged — the camera permission is the rep's grant, not an OS-picker auto-routing. | OS-level semantics; documented in the runbook. |
| T-S39 | Sync chip false-Synced — the chip reads from local state and shows "Synced" while 5 operations are in flight; the rep believes everything is safe and powers down the phone (ADR-0009 §3.11) | ADR-0009 §3.11: chip state machine reads from Dexie's `_sync_state` aggregate (counts of `dirty_*` + `conflicting` + dead-letter rows). "Synced" requires ZERO non-clean rows AND the queue worker is idle AND the last health probe succeeded. Operations in flight keep the chip on "Syncing" until the response lands AND the entity row is marked clean in the same transaction (Dexie + queue row delete are one Dexie transaction). The 30-second poll catches a stuck worker. | None material — the chip is read-from-Dexie atomic with the drain. |
| T-S40 | Pull-to-refresh DoS — a rep mashes the refresh trigger; the queue worker fires multiple concurrent drains; two drains race the same queue row, sending the operation twice (ADR-0009 §3.2 + §3.11) | ADR-0009 §3.2: the queue worker is single-thread + has a `running` flag that short-circuits concurrent invocations — a second pull-to-refresh while a drain is in flight is a no-op. The hot loop's `SELECT … LIMIT 10` + per-row processing is sequential. Even if a duplicate fire happened, the idempotency middleware (T-S4) catches the same `payload_hash` on the server. Integration test asserts ten concurrent pull-to-refresh fires produce exactly one drain pass. | None material — single-thread + idempotency are belt-and-suspenders. |
| T-S41 | A rep ignores the dead-letter UI for >48h after a critical operation (signed recommendation, signed inspection) lands in dead-letter; the chain anchor never fires and the operation is evidentially lost (ADR-0009 §3.11 + CLAUDE.md #2) | ADR-0009 §3.11: dead-letter rows surface in the sync-status chip's amber "Paused" state with the count of conflicts + dead-letter rows. Tap-to-open shows each row with the server's error code + a "retry" / "fix" / "delete local" action. The rep is the authority on recovery. For signed operations (recommendation submit, inspection signature), the local Dexie row carries the rep-trusted signature data + ciphertext; a rep who needs to recover the operation has the bytes — they can copy the body into a fresh draft + re-submit. The runbook covers common error codes (citation_marker_density_violation, citation_not_in_corpus, signatures_incomplete, idempotency_key_payload_mismatch) and the operator-side recovery procedure. | Rep discipline is the bound; the chain captures only what actually landed, by design (non-negotiable #2). |
| T-S42 | The per-route `clientId` + `Idempotency-Key` + `If-Match` ratchet is incomplete — one POST route in 1.5–1.9 doesn't honor `clientId`, the offline-write path for that entity silently fails (the optimistic local id is server-overwritten on drain, breaking FK references in dependent local rows) (ADR-0009 §3.13) | ADR-0009 §3.13 S2: every existing POST handler is ratcheted in the same slice; the route-table in §3.3 enumerates the nine handlers that take `clientId`. Integration tests assert that for each route, a POST with `clientId` returns a row with `id = clientId`; a POST without `clientId` still works (Zod `.optional()`). The CI lint rule (1.6 sec-F3 pattern) catches a new POST handler that doesn't import the `clientIdSchema`. | A future POST handler that forgets the ratchet is caught by the lint rule + the integration test. |
| T-S43 | The new `sync_idempotency` migration runs out of order with the 1.5–1.9 route ratchets — production deploys the route changes before the table exists, idempotency middleware crashes on every request (ADR-0009 §3.13) | ADR-0009 §3.13 S1 + S2: the migration lands in S1 (Dexie schema + migration 0009 + the `version` column on every updatable entity table); S2 wires the middleware + the route ratchets AFTER the migration is in place. CLAUDE.md "migrations are append-only" honored. The deploy sequence is migrations → API binary → web bundle; the middleware gracefully no-ops on missing `Idempotency-Key` header (GET/HEAD too) so a stale client during the deploy window is fine. | Deploy sequencing is the bound; documented in the runbook. |
| T-S44 | A Dexie schema migration between app versions (1.10 → 1.11 / Excel imports) loses an in-flight queued operation because the migration drops or renames a column that the queued payload references (ADR-0009 §3.1 + CLAUDE.md "migrations are append-only") | ADR-0009 §3.1: Dexie schema follows the same append-only posture as the server migrations — `db.version(N).stores({...}).upgrade(tx => ...)` adds tables and columns; existing schemas remain valid. A queued operation's `payload_ct` is opaque ciphertext addressed by `payload_hash`; the queue worker doesn't introspect it. A schema bump that renames a Dexie table (which the ADR doesn't do) would migrate the queue's `table` column via the upgrade hook. The 1.10 → 1.11 path is a v2 schema add (Excel import tables), not a rename. | Append-only discipline is the bound; documented. |
| T-S45 | First-run cannot complete offline — the workplace KEK + signing key bootstrap is server-side; a rep installing the PWA on a fresh device with no network is stuck (ADR-0009 §3.6 + 1.2 T-A11) | ADR-0009 §3.6 + §"Require-online": all `/api/auth/*` routes (passkey verify, password verify, TOTP verify, recovery codes, first-run, session refresh — except `/session` which is cached for returning reps) are require-online. The first-run flow shows a clear "Network required to set up the app" banner. A returning rep with a valid cached session can boot to the app shell + Dexie cache offline; a fresh install cannot. The runbook covers the first-run requirement. | Documented; no security gap — the offline-first promise is for ongoing use, not bootstrap. |
| T-S46 | Three concurrent retry curves at play — per-mutation backoff (1s→24h), per-evidence-upload three-step retry, per-session-token JWT refresh — interact pathologically (e.g., a refresh failure during a drain leaves the queue stuck on 401 with no recovery) (ADR-0009 §3.2 + §3.8 + 1.2 §3) | ADR-0009 §3.2: a queue 401 is treated as session-expired; the queue worker pauses (no more drain attempts), the UI surfaces "Session expired — sign in to resume sync" in the chip, the rep re-authenticates (online), the access JWT refreshes, the queue resumes from the same backoff cursor (the `attempt_count` is NOT incremented by a 401 — only by 5xx / network). The session refresh path itself is require-online (§3.6); a failure there is a separate UX flow. Evidence-upload three-step retry interacts only with its own `step` counter — the per-mutation backoff curve covers the overall row. Integration test asserts a 401 mid-drain pauses cleanly + resumes after re-auth. | Documented; the three curves are decoupled by status code. |
| T-S47 | The chain anchor's timestamp diverges from the rep's reality across every queued operation, producing two timestamps in evidence (chain `ts_ms` + queue-typed-at on the device clock) — an arbitration adjudicator unfamiliar with the offline-first posture may attribute the divergence to tampering (CLAUDE.md #2 + ADR-0009 §3.2) | ADR-0009 §3.2 + §3.12 + Consequences: the runbook documents the chain-anchor latency contract explicitly — the chain proves "the server received this at time T," NOT "the rep typed this at time T'." The dead-letter UI and the sync-status view both surface the queue-typed-at hint from `sync_queue.created_at` (device clock) so the rep can produce it as a separate timeline if needed. The chain is the canonical evidentiary surface; the device clock is corroborating context. Arbitration adjudicators are guided by the runbook §"Chain-anchor latency framing." | Documented; the runbook is the arbitration-side resource. |
| T-S48 | A `recommendation.submit` conflict on two offline devices produces two chain anchors for what is evidentially the same recommendation; the resolution path emits a `recommendation.withdrawn` chain row for the duplicate, leaving TWO chain rows for one logical submission (ADR-0009 §3.7 + 1.9 T-R20) | ADR-0009 §3.7: the chain captures both — the duplicate submit AND the withdrawal of the duplicate. The runbook frames this as the correct evidentiary surface: the rep submitted twice (intentionally or not), and the chain records both events truthfully. The 1.9 T-R20 single-co-chair scope makes this near-zero in practice. The audit-log-verify cross-check (1.3 T-AC1) catches any tampering with either chain row. | Documented; near-zero in single-co-chair scope. |
| T-S49 | A conflict on encrypted text requires step-up to view both versions; a rep without recent step-up cannot resolve and the operation stays in `sync_conflicts` indefinitely (ADR-0009 §3.7 + §3.8) | ADR-0009 §3.7 + §3.8: the conflict UI for encrypted fields fires the existing reveal endpoints (`POST /api/sync-conflicts/:id/reveal` with `{which: 'local'|'server'|'base'}`); each reveal is step-up gated (same shape as 1.5 T-H4 / 1.8 T-I12 / 1.9 T-R26). The rep must be online + step-up-fresh to resolve. The amber chip surfaces the conflict count; an unresolved conflict has no impact on the rest of the app — the rep can keep working on other entities. The new `sync_conflict.revealed` chain kind anchors each reveal (CLAUDE.md #2). | Operational — a rep without step-up can't resolve, but the conflict doesn't decay or auto-merge. Documented. |
| T-S50 | The rep's device clock is set wrong — the `sync_queue.created_at` (device clock) is hours or days off; the dead-letter UI hint is misleading; an arbitration timeline derived from device clock is wrong (ADR-0009 §3.2 + Risks) | ADR-0009 Risks: the chain anchor `ts_ms` is server-side and unaffected by device clock skew — the canonical evidentiary timestamp is correct regardless. The device clock skew shows up only on the rep-facing UI hint (queue-typed-at) and is a known limitation. The runbook covers the framing: "the chain is canonical; the device clock is rep-facing only." | Server-side timestamps are the bound; documented. |

**Audit hooks for sync.** **No new chain-anchored audit kinds in 1.10.** The existing 1.5–1.9 kinds (`hazard.created`, `hazard.status_changed`, `action_item.created`, `action_item.moved`, `action_item.move_undone`, `action_item.updated`, `evidence.uploaded`, `evidence.read`, `evidence.list_accessed`, `inspection.created`, `inspection_finding.created`, `inspection_finding.promoted`, `inspection_finding.read`, `inspection.signed`, `inspection.exported`, `inspection.export.downloaded`, `audit.inspection_template.seeded`, `recommendation.drafted`, `recommendation.draft_patched`, `recommendation.submitted`, `recommendation.response_captured`, `recommendation.resolved`, `recommendation.withdrawn`, `recommendation.exported`, `recommendation.export.downloaded`, `audit.workplace_signing_key.seeded`) all fire at SERVER drain time — when the queue worker successfully ships the operation and the route handler runs inside its db.transaction. The chain anchor never fires at local-write time on the device; the local Dexie row is rep-trusted until the drain lands. Two operational kinds are added but are documented as separate non-chain-anchored streams: `sync_conflict.revealed` (the conflict-resolution reveal endpoint, mirrors the 1.5 reporter-identity reveal pattern from T-H4 — chain-anchored per CLAUDE.md #2 because it's a step-up-gated decrypt with audit value); and `sync.idempotency_replayed` (a debug-only operational stream, NOT chain-anchored — same shape as `auth_events` from the 1.2 → 1.3 backfill, and S5 reviewers may drop it without ceremony if the operational value doesn't justify the noise).

**The cross-cutting semantic shift** affects every prior kind: the chain row's `ts_ms` now represents "server received at" rather than "rep typed at." For online operations (which remain the overwhelming majority at single-co-chair scope) the two timestamps differ by milliseconds — the existing arbitration framing holds. For offline-then-drained operations the divergence can be hours or days; the runbook (`docs/runbooks/offline-sync.md` §"Chain-anchor latency framing") leads with this. Two specific kinds carry rep-facing legal weight that pivots on the new semantics:

- **`recommendation.submitted`** — the 21-day s.9(21) clock is measured from chain `ts_ms` (server `submitted_at`), NOT from device queue-typed-at, per §3.12. `computeRecommendationDeadline` (1.9 T-R10, pure function in `packages/shared-types`) consumes the server timestamp only. Rep-facing copy in the recommendation detail view makes this explicit ("21-day clock starts on sync").
- **`inspection.signed`** — the signature timestamp recorded in the chain is server-drain time. The runbook documents the arbitration framing: the device clock + the queue-typed-at hint on `sync_queue.created_at` are corroborating evidence the rep can produce, but the chain is canonical.

No payload-shape changes to any existing kind (the 1.5–1.9 typed-emit invariant holds — CLAUDE.md #2 + 1.3 priv-F2). Two new event-shape additions land in `packages/shared-types`: `SyncOpKind`, `SyncEntityTable`, `SyncOperationStatus`, `SyncConflictKind` (all enum/literal types — no PI surface). Verification: `audit-log-verify` does not change; the chain itself is unchanged in shape. The `sync_idempotency` ledger is operational-only and is NOT cross-checked by the verifier (the chain remains the canonical evidentiary record).

---

## 3. Security Controls

### Authentication

- **Primary:** Passkey/WebAuthn (FIDO2). Phishing-resistant. Bound to origin.
- **Fallback:** Username + password (Argon2id: 64MB mem, 3 iter) + mandatory TOTP (RFC 6238).
- **Session:** JWT (EdDSA, 30 min access) + refresh token (HttpOnly, Secure, SameSite=Strict, 14 days). Rotates on use.
- **Step-up auth:** Re-prompt for passkey/TOTP before exports, identity decryption, deletions, **Excel import commits, Excel import reverts, action item archival**.
- **Device registration:** Optional but tenant admin can require it.
- **Brute force protection:** 5 failed = 15 min lockout; 10 = 1 hour; 20 = manual unlock required.

### Encryption

- **In transit:** TLS 1.3 only. HSTS preload. No HTTP fallback.
- **At rest (sensitive fields):** XChaCha20-Poly1305 via libsodium, application-layer, before Postgres write.
- **At rest (files):** Client-side encryption before Tigris upload. Tigris stores ciphertext only.
- **Local cache:** IndexedDB sensitive fields encrypted with session-derived key. Cache cleared on logout.
- **Master key:** Fly Secrets, accessible only to `api` Machine process. Never logged. Never returned by API.
- **Key rotation:** Annual rotation of derived keys. Master key rotation triggers re-encryption sweep.
- **Excel import encryption:** Sensitive fields encrypted in the browser via libsodium-wrappers BEFORE any API call. The raw Excel file never leaves the device.

### Tenant Boundaries (Single-Tenant)

User-level access controls still apply:

- Every query is scoped to the authenticated user's role
- A `worker_rep` cannot read another rep's practice journal
- A `read_only` reviewer cannot decrypt member identities without re-consent
- Audit log writes are immutable; users cannot delete their own entries
- **Action item moves cannot be silently rolled back — every move is permanent in the audit log even if the item is moved again**

### Audit & Logging

- **Tamper-evident audit log** (hash chain, HMAC-seeded).
- **What gets logged:** all writes to sensitive tables, all reads of decrypted sensitive fields, all exports, all auth events, all config changes, **all Excel import lifecycle events, all action item section moves**.
- **What does NOT get logged:** sensitive field contents, encryption keys, passwords, full request bodies for sensitive endpoints. Logs contain identifiers and metadata only.
- **Log retention:**
  - **`audit_log` (tamper-evident chain):** rows retained **indefinitely** (the chain is the evidentiary record). The `ip` and `user_agent` columns redact to `NULL` at 1 year via the annual sweep + `audit.ip_redacted` chain anchor — see `.context/decisions.md` 2026-05-29 entry and `docs/runbooks/auth.md` §4a/§7. The redaction script lands in 1.12 hardening; until then, IP/UA persist for the full chain age (documented residual).
  - **`auth_events` (1.2 flat table):** read-only legacy preserved for hash-anchored reference from the 1.3 backfill anchor. Same retention posture as `audit_log`.
  - **`login_attempts` and `webauthn_challenges`:** operational; pruned by `auth-retention.ts` (2× hard-tier window for `login_attempts`, expires_at + 1h grace for `webauthn_challenges`).
  - **Application logs (Pino):** 90 days hot, 1 year cold per the pre-1.3 baseline.
- **Verification:** `scripts/audit-log-verify.ts` runs nightly via cron.

### Headers & Transport

(Unchanged. CSP strict mode, HSTS preload, Permissions-Policy blocking unneeded APIs, COEP/COOP for cross-origin isolation.)

### Input Validation

- All API inputs validated with Zod schemas
- Server-side validation is the source of truth
- File uploads (evidence): type-sniffed, size-limited (50 MB default), hashed
- HTML user input rendered with proper escaping; rich text passed through DOMPurify
- **Excel parsing: see § Excel Import Security below**

### Rate Limiting

- Auth endpoints: 10 requests / minute / IP
- API write endpoints: 60 / minute / user
- AI proxy: 30 / hour / user
- Export endpoints: 5 / hour / user
- **Excel import preview: 10 / hour / user** (preview is cheap; commits require step-up auth anyway)

### Dependency & Supply Chain

- `pnpm audit` runs on every CI build; high/critical vulns block merge
- Dependabot enabled
- Gitleaks scans every push for committed secrets
- Lockfile committed; changes reviewed
- Minimal dependency footprint
- No analytics SDKs, no telemetry, no third-party tracking
- **SheetJS is the only Excel-parsing dependency; pinned to a specific version with regular review of CVEs**

### Browser & Mobile Hardening

(Unchanged. Service worker integrity checks, IndexedDB cleared on logout, camera-captured photos never written to device library, biometric prompt after idle, auto-logout.)

---

## 4. Excel Import Security

### Why This Needs Its Own Section

Excel files are an attack surface. .xlsm files can contain VBA macros (we don't execute them, but the file can also contain crafted content that targets parser bugs). Even .xlsx files can contain XXE attacks, zip bomb attacks, formula injection attacks, and embedded executables.

The fact that we parse client-side instead of server-side is a meaningful mitigation — a parser exploit attacks one rep's browser, not the whole platform. But we still defend.

### Supported File Family

One file family is recognized and parsed; everything else is rejected:

1. **Minutes files** (.xlsm or .xlsx) — meeting minutes with action item tracking

Inspection file imports are not supported in Release 1. Inspections are conducted natively in-app.

### Controls

1. **Pure parsing only.** SheetJS is configured with `cellFormula: false` and `cellHTML: false`. We never evaluate formulas or interpret HTML in cells.
2. **No macro execution.** VBA macros in .xlsm files are ignored. They are not interpreted, not displayed, not stored.
3. **Zip bomb protection.** Files >10 MB rejected at file-picker time. Decompressed size capped at 100 MB.
4. **Schema-first parsing.** We don't attempt to "interpret" arbitrary Excel files. The parser looks for our documented schema (`docs/excel-import-format.md`) and rejects anything that doesn't match. Files that look hostile but happen to match the schema would have to look like valid JHSC minutes — a high bar.
5. **Sanitization at parse.** Cell values are coerced to expected types. Strings are trimmed and length-capped (descriptions: 5000 chars, names: 200 chars).
6. **Formula injection prevention.** If a string cell begins with `=`, `+`, `-`, `@`, or tab, prepend a single quote when storing. This is defense for any downstream Excel exports.
7. **HTML injection prevention.** All imported text is treated as plain text and properly escaped wherever rendered.
8. **Hash-based provenance.** The SHA-256 of the imported file is stored (not the file itself). This lets us prove later "this action item came from this specific file at this time."
9. **No execution of imported content.** Imported text is never `eval`'d, never rendered as HTML without escaping, never used as a template string, never passed to a query builder as raw SQL.
10. **Preview before commit.** Every import shows the rep what will happen before anything is created. Conflicts and surprises surface to the rep.
11. **Reversible for 30 days.** If a malicious import is discovered, it can be reverted within 30 days (creates an audit entry of the revert).
12. **No bulk auto-import.** Files must be selected one at a time via file picker. No "watch this folder" automation.
13. **PII scanning on imports.** Each imported field is run through the client-side PII heuristic. Fields detected as likely containing names or identifying info are encrypted before they leave the browser.

### What We Don't Defend Against

- A rep importing their own legitimate file with embedded names of real workers. That's the intended use case. The fact that names are in the file means they need to be encrypted, but not blocked.
- A rep importing a file given to them by a third party who modified it maliciously. We trust the rep's choice of file. We do warn (via the file hash and source documentation) that imports are tied to specific source files.
- An attacker with rep credentials importing a malicious file. The step-up auth on commit, the preview, and the audit log are the mitigations here.

---

## 4a. Export Security

Inspection PDF exports are a new attack surface — once a document leaves the app, the rep is responsible for handling it. The app's job is to make exports auditable and tamper-evident.

### Controls

1. **Step-up auth before export.** Re-authentication required (passkey or TOTP) before generating any PDF.
2. **Audit-logged.** Every export creates an `export_records` entry with: exporter identity, exported_at timestamp, IDs of records included, output document hash (SHA-256), audit chain anchor. The audit entry is immutable.
3. **Tamper-evident output.** Every generated PDF includes a footer on every page:
   - Exported by [user] on [ISO date]
   - Document hash: sha256 [hash]
   - Audit anchor: [hash]
   This makes after-the-fact modification detectable.
4. **Decryption is in-memory only.** Sensitive fields are decrypted only long enough to render to PDF, then zeroed in memory. No plaintext copy persists.
5. **Rate-limited.** Maximum 5 exports per hour per user (matches general export rate limit). Batch exports capped at 100 inspections per call.
6. **No background or scheduled exports.** Every export is initiated by a logged-in user with step-up auth. No cron-job exports, no automatic "send me my data weekly" features.
7. **Photos in PDFs are decrypted and embedded** — this is the necessary purpose of export. The footer disclosure makes downstream leaks traceable to the exporter.
8. **Exports do not include audit log content** by default. The audit chain anchor is included so the chain can be verified, but the chain itself stays in the database. A separate audit-log export endpoint exists for the rare case where the chain must travel with the document.

### What This Doesn't Defend Against

- A rep exporting and then carelessly sharing a PDF. Once the PDF leaves the app, it's the rep's responsibility.
- A subpoenaed disclosure of an exported PDF. The export was authorized by the rep at the time; the audit log proves what was exported. If the rep is later forced to disclose, the disclosure is the issue, not the export.
- An attacker with rep credentials exporting and exfiltrating. The step-up auth and rate limits are the mitigations; the audit log lets you detect this after the fact.

---

## 5. Action Item Move Integrity

The action item section lifecycle is operationally critical. Each move (e.g., New Business → Old Business) is:

1. **Recorded in `action_item_moves`** with timestamp, actor, from/to sections, optional reason
2. **Linked into the audit chain** with hash continuity
3. **Counter-signed by the meeting context** — moves happen "in a meeting," and the meeting ID is part of the audit metadata
4. **Subject to step-up auth for archival** — moving an item out of normal view (to "archived") requires re-authentication

Why this matters: a rep needs to be able to prove later, in front of an arbitrator, that an item was raised on date X, moved to old business on date Y, and closed on date Z. The audit chain provides that proof. Tamper would be detected by the chain verification routine.

---

## 6. Privacy Controls

(Largely unchanged. Data minimization, consent capture, pseudonymization at intake, PIPEDA rights.)

### Excel Import Privacy

- **The raw Excel file never leaves the device.** It's parsed in the browser.
- **Imported content is encrypted at the application layer** before any sync to the server.
- **Imports respect the same retention rules** as direct data entry. If a record is purged, its source-file reference is purged too.
- **A rep can revert an import** to remove all records that came from a specific file. The audit log retains the revert event but the content is cryptographically erased.

### Cross-Border Disclosure (Honest Statement)

Fly.io and Neon are US-incorporated. CLOUD Act applies. The encryption mitigates substantially — a compelled disclosure yields ciphertext for sensitive fields. **Excel import data is in the same category** — once it's in the database, it benefits from the same encryption guarantees and faces the same theoretical compelled-disclosure exposure.

---

## 7. Incident Response

(Unchanged from prior spec. P0–P3 classification, detect/triage/contain/eradicate/recover/notify/post-mortem.)

### New Incident Types

- **Malicious Excel import discovered** — treat as P2 unless the import affected the audit chain (then P0). Revert the import. Audit affected records. Notify the rep.
- **Action item move integrity failure** — audit chain verification finds a missing or inconsistent move. Treat as P1. Investigate, restore from chain backup if possible, document.
- **PII heuristic false negative on import** — sensitive content imported in plaintext column. Treat as P1. Immediately encrypt affected fields, re-key if needed, audit the import.

### Breach Notification

PIPEDA s.10.1 still applies. For JHSC worker data, the threshold is generally met if member identities or medical information are exposed, including via a compromised import.

### Backup & Recovery

- Neon point-in-time recovery: 7-30 days
- Nightly encrypted `pg_dump` to Tigris
- Monthly local cold-storage copy
- Quarterly restoration drill
- **Action item move history is included in all backups and verified during restoration drills**

---

## 8. Pre-Launch Security Checklist

Before going live with real worker data:

- [ ] All sensitive fields encrypted at application layer (verified by Neon query inspection)
- [ ] Audit log verification passes on full dataset
- [ ] CSP strict mode active, no inline scripts
- [ ] All security headers verified (observatory.mozilla.org)
- [ ] HSTS preload submitted
- [ ] Passkey enrollment tested on iOS and Android
- [ ] TOTP fallback tested
- [ ] Step-up auth tested for all sensitive operations
- [ ] Rate limits verified
- [ ] Backup and restore tested end-to-end
- [ ] Disaster recovery: full app rebuilt from scratch using only backups
- [ ] `pnpm audit` clean
- [ ] Gitleaks clean
- [ ] Penetration test attempted (OWASP ZAP minimum)
- [ ] Incident response playbook reviewed
- [ ] Privacy notice drafted
- [ ] Cold-storage backup procedure tested
- [ ] Logout / session expiry tested mobile + desktop
- [ ] Biometric re-auth tested after idle timeout
- [ ] Workplace config loads from env, not committed
- [ ] Camera photos bypass device library (verified)
- [ ] **Excel import: malicious file fuzzing performed against parser**
- [ ] **Excel import: zip bomb rejection tested with crafted file**
- [ ] **Excel import: formula injection prevention verified in downstream exports**
- [ ] **Excel import: PII heuristic tested with realistic minutes content**
- [ ] **Excel import: preview-then-commit flow tested end-to-end**
- [ ] **Excel import: reversal within 30 days tested with audit log verification**
- [ ] **Action item move audit chain verified across 100+ simulated moves**
- [ ] **Inspection template versioning tested — historical inspection rendered correctly under old template version after template upgrade**
- [ ] **Inspection manual promotion tested — one-tap promotion creates correctly-linked action item in new_business section**
- [ ] **Inspection PDF export — single inspection produces evidence-grade output with hash footer**
- [ ] **Inspection PDF export — date-range batch (100 inspections) renders correctly with table of contents**
- [ ] **Inspection PDF export — step-up auth enforced, audit log entry created with output hash**
- [ ] **Inspection PDF export — verified that modifying the exported PDF causes hash mismatch detectable on re-verify**

---

## 9. Ongoing Security Operations

(Unchanged: weekly audit log review, dependency checks, monthly audit, quarterly pen test, annual key rotation.)

Additions:

- **Monthly:** Review Excel import logs. Confirm no unexpected high-volume imports.
- **Quarterly:** Re-fuzz the Excel parser with the latest SheetJS version.
- **Annually:** Review the schema in `docs/excel-import-format.md` against actual workplace files. Update as the rep's workflow evolves.
