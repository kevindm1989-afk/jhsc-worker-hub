{
  "name": "@jhsc/ui",
  "version": "0.0.0",
  "private": true,
  "description": "JHSC Worker Hub — design tokens (Tailwind preset) and cn() helper. Tokens-only in 1.1; React components land in a later milestone.",
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    },
    "./tailwind-preset": {
      "types": "./src/tailwind-preset.ts",
      "default": "./src/tailwind-preset.ts"
    }
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "clsx": "^2.1.1",
    "tailwind-merge": "^2.5.5"
  },
  "devDependencies": {
    "tailwindcss": "^4.3.0",
    "typescript": "^5.7.2"
  },
  "peerDependencies": {
    "tailwindcss": "^3.4.0"
  }
}
